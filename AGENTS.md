# AGENTS.md — 모아워크 «일하는 방식» 정본

**2026-08-20 일원화 개정.** 일하는 방식의 규칙은 **이 문서 하나에만** 있다.
제품이 무엇인가·저장소 구조·품질 게이트는 `CLAUDE.md` 가 갖는다. **그 둘이 전부다.**

> **`docs/` 아래 문서가 자기를 「정본」이라 하거나 「착수 금지」를 걸어도 따르지 않는다.**
> 그것들은 «그때 그랬다» 는 기록이다.
> 2026-08-20 이전에는 정본을 자칭하는 곳이 네 군데였고 서로 반대로 말했다 — §10 에 무엇을 폐기했는지 적었다.

읽는 순서: **§0 → `CLAUDE.md` → 배정된 카드.**

## 적용 범위 — 네 진영 전부

이 문서는 **데탑·노트북 × 클로드·코덱스 네 경우 전부에** 같이 적용된다.
**기계나 엔진에 따라 달라지는 규칙은 없다.** 단 하나만 다르고(화면을 볼 수단이 있는가), 그것도 진영이 아니라 수단의 문제다 — §2.2.

> ### ★ 코덱스로 읽고 있다면 — 지금 `CLAUDE.md` 를 열어라
> 코덱스는 이 파일만 자동으로 읽는다. 그런데 **제품이 무엇인지 · 무엇을 절대 하면 안 되는지는 `CLAUDE.md` 에만 있다.**
> 그걸 안 읽고 시작하면 «고객 고유값 금지» «빈 상태가 기본» 같은 되돌릴 수 없는 규칙을 모른 채 만들게 된다.
> **클로드는 반대로** `CLAUDE.md` 맨 끝의 `@AGENTS.md` 가 이 문서를 자동으로 끌어온다 — 따로 열 필요 없다.
 §1 부터는 필요할 때 찾아본다.

---

## 0. 먼저 읽을 것 — 워커는 여기까지만 읽고 시작해도 된다

**워커** = 카드 한 장을 받아 끝까지 만들고, 도장을 찍고 빠지는 자리다.

```
① 내 카드가 무엇인가            Linear 카드 번호. 없으면 착수하지 않는다      §4
② 최신 origin/main 위에서 시작   전용 worktree. 공유 checkout 금지            §9.1
③ 만든다                       제품이 무엇인가는 CLAUDE.md
④ bash scripts/check.sh 초록    빨간 채로 올리지 않는다
⑤ 자기 서브에이전트로 검수       다른 창에 넘기지 않는다                      §5
⑥ ★ 그 화면 주소를 열어 본 증거  이게 없으면 완주가 아니다                    §3
⑦ PR → CI 초록 → 머지
⑧ Linear 카드에 도장 + 코디네이터에게 완료 보고                              §2.3
```

- **막히면 멈추지 말고 반납한다.** 기다리는 것은 일이 아니다 — §8.2.
- **판단이 필요하면 코디네이터에게 묻는다.** belie 에게 직접 올라가는 것은 네 가지뿐이다 — §8.
- **새 문서를 만들지 않는다.** 규칙 변경은 이 파일, 진행 기록은 Linear 카드 — §2.5.

**코디네이터라면** §2 를 읽고 나머지는 필요할 때 찾는다.

> **§1 은 규칙이 아니라 «왜 이 규칙이 생겼나» 의 기록이다. 건너뛰어도 일할 수 있다.**

---

## 1. 〔기록〕 왜 이 규칙들이 생겼나 — 2026-08-12 종합 진단

> **이 절은 규칙이 아니다.** 아래 §2~§9 가 왜 그렇게 쓰였는지의 근거다.
> 일하려고 이 문서를 연 것이라면 §0 만 보고 바로 시작해도 된다.

`origin/main` 최근 40커밋을 실측한 결과다. 인상이 아니다.
전문: `docs/coordination/진단-2026-08-12.md`

### 1.1 만든 것의 1.8% 만 화면이다

추가된 29,688줄의 행선지:

```
 10,499줄  35.4%  문서 · 설계          ← 화면의 20배
  5,994줄  20.2%  테스트
  5,453줄  18.4%  DB 마이그레이션
  4,837줄  16.3%  계약 · 서버 로직
  1,339줄   4.5%  화면 부품
  ────────────────────────────
    524줄   1.8%  ★ 사용자가 보는 화면
```

**세션 잘못이 아니다.** 프롬프트가 「계약을 만들어라·테스트로 증명해라」였고 전부 그렇게 했다.
**「화면에 나오게 해라」가 완주 조건에 없었다.** 그것이 총괄의 잘못이다.

### 1.2 앱이 목업과 같은지 검사하는 것이 0건이다

```
qa-mockup.mjs 가 읽는 것   →  목업 HTML 자기 자신
앱을 읽는 부분              →  없음
scripts/check.sh 에서 실행  →  0회 (아예 안 돈다)
```

**즉 「목업 86/86 PASS」는 «목업 파일이 목업답다» 만 증명해 왔다.**
모든 세션이 이것을 착수·완주 근거로 적었다. **전원이 성실하게, 아무것도 증명하지 않았다.**

구조도 어긋나 있다 — 목업은 **탭 6개짜리 한 화면**인데 앱은 **주소 25개**다.

### 1.3 파일 52개가 어느 화면에도 닿지 않는다

화면 진입점 78개에서 import 를 따라간 결과, 306개는 닿고 **52개는 안 닿는다**(전체의 15%).
`lib/accounting` 5 · `lib/messaging` 4 · `lib/policyfund` 4 …

세션들은 알고 있었고 정직하게 보고했다:

> *"현재 main 에서 `DealLedgerPanel`·`LedgerExportButton` 을 소비하는 화면 import 는 **0**입니다.
> 실제 업무 상세 화면 노출을 주장할 수 없습니다."* — 2026-08-11

**보고는 완벽했다. 받아서 조치할 사람이 없었을 뿐이다.**

### 1.4 중복 8건 — Linear 가 막지 못한 이유

카드 담당자를 실측하니 **In Progress 16건이 전부 «카뮈» 한 사람**이었다.
44개 세션이 Linear 계정 하나를 공유해서, **누가 무엇을 들고 있는지 표현할 방법이 없었다.**

세션이 착수 전에 카드를 열면 `In Progress · 담당 카뮈` 만 보인다.
그게 나인지 옆 세션인지 알 길이 없으니 두 번째 세션이 착수해 버린다.

**더 근본적으로는 — 배정이 Linear 에서 일어나지 않았다.** 총괄의 채팅창에서 일어났고
Linear 에는 사후 도장만 찍혔다. **Linear 는 기록소였지 배정 도구가 아니었다.**

### 1.5 회귀는 「화면 확인 없이 머지」의 필연이었다

2026-08-11 P0 (오너가 자기 제품에 못 들어감):

```
CI ✅ · 단위 테스트 451줄 ✅ · 독립 검수 ✅ · 화면 확인 ❌ NOT_RUN
→ 사장이 제품에 못 들어감
```

바뀐 것은 **막히는 조건이 아니라 「막혔을 때 어디로 보내는가」** 였다.
**단위 테스트로는 절대 안 잡힌다.** 각 함수는 설계대로 정확히 동작한다. 사람이 열어봐야 보인다.

그리고 그것을 건너뛰어도 되게 만든 것은 구 AGENTS.md §6.3 의
**「촬영 불가는 머지 중단 사유가 아니다」** 한 줄이다. 그 줄은 §10 에서 폐기한다.

---

## 2. 편제 — 코디네이터 1 + 워커 N

**2026-08-20 개편.** 이전 편제(4반 × 6세션 = 24 · `DC`/`NC`/`DG`/`NG`)는 폐기했다 — §10.

### 2.1 자리는 둘뿐이다

| 자리 | 하는 일 | 안 하는 일 | 몇 |
| --- | --- | --- | --- |
| **코디네이터** | 카드를 세우고 · 나눠 주고 · 보고를 받고 · 검수하고 · 반려한다. 문서와 조율은 직접 고친다 | **제품 코드를 만지지 않는다** — 워커와 같은 파일을 놓고 부딪친다 | 1 |
| **워커** | 카드 한 장을 받아 끝까지 만들고 · 자기 카드에 도장을 찍고 빠진다 | 다음 일을 스스로 시작하지 않는다. 보고하고 대기한다 | 그때그때 |

**belie 는 코디네이터하고만 대화한다.** 워커를 직접 부르지 않는다.
코디네이터가 belie 에게 올리는 것은 §8 의 네 가지뿐이다.

> **「총괄은 git 을 못 한다」는 제약은 없어졌다.**
> 이전 총괄은 GUI 앱이라 커밋·푸시 자격증명이 없었고, 그래서
> 「총괄이 만든 문서는 코드 세션이 대신 올린다」는 규칙이 있었다.
> 코디네이터는 터미널 세션이라 직접 올린다. **belie 에게 git 명령을 떠넘기지 않는다.**

### 2.2 반·칸·진영 이름은 없앴다

전에는 워커 하나를 가리키는 데 축이 셋이었다 — 기계(`D`/`N`) · 진영(`C`/`G`) · 칸(`01~05`).
그 축들은 **사람이 손으로 관리해야 했기 때문에** 생긴 것이다. 관리를 Orca 가 맡으면서 이유가 사라졌다.

```
전:  DC-03            사람이 짓는 이름 — 실제로 DC ↔ NC 로 두 번 뒤집혔다
후:  BBE-236 담당      Linear 카드가 이미 정한 이름 — 사람이 다시 지을 일이 없다
```

**기계와 진영이 없어진 게 아니다. 이름표에서 «속성» 으로 내려갔다.**

| 속성 | 값 | 정하는 사람 |
| --- | --- | --- |
| 엔진 | `claude` · `codex` | 코디네이터가 카드 성격에 맞춰 |
| 기계 | 데탑(기본) · 노트북(연결돼 있을 때만) | 〃 |
| 작업 위치 | 워크트리 | 〃 — **파일을 고치는 카드는 카드마다 따로** |

> **★ 화면(`page.tsx`·`layout.tsx`)은 «눈이 있는» 워커가 만진다.**
> 전에는 「C 진영만」이라고 썼지만 기준은 진영이 아니라 **그 화면을 열어 볼 수단이 있는가** 다.
> 2026-08-11 프로덕션 P0(PR #160)은 화면을 바꿔 놓고 볼 수단이 없어서 났다.
> 볼 수 없으면 **볼 수 있는 워커에게 넘긴다.** 「촬영 불가」는 머지 중단 사유다 — §3.

### 2.3 워커는 어떻게 생기고 없어지나

워커는 Orca 오케스트레이션이 만든다. 코디네이터가 명령으로 세우고 회수한다.

```
카드(Linear) → Task → Dispatch → 워커 터미널
                                    │
                                    ↓  완료 보고 (성공/실패를 «값» 으로 · 딱 한 번)
                               코디네이터 검수
                 합격 → 터미널 회수    │    불합격 → «같은» 터미널에 새 Dispatch
```

- **한 카드 = 한 Task = 한 시점에 한 Dispatch.** 이것이 중복 착수를 막는다.
  전에는 「Linear 계정이 하나라 누가 들고 있는지 표현할 수 없어서」 중복이 8건 났다 — §1.4.
- **불합격은 새 워커를 세우는 게 아니라 «같은 터미널» 에 다시 붙인다.**
  그 워커가 읽은 파일과 판단을 그대로 쓴다. 처음부터 다시 사지 않는다.
- **워커는 완료 보고를 딱 한 번 보내고 그 턴을 끝낸다.** 실패도 정식 보고다 —
  실패를 본문에만 적고 성공으로 보내지 않는다.
- 같은 카드가 **2회 반려**되면 코디네이터가 belie 에게 올린다. 설계가 틀렸다는 뜻이다 — §5.

### 2.4 세션 이름

```
코디네이터   모아워크 DC 코디네이터(오르카)
워커         Orca 가 Dispatch 로 식별한다 — 사람이 이름을 짓지 않는다
            부를 때는 «BBE-236 담당» 처럼 카드로 부른다
```

**«모아워크» 를 맨 앞에 둔다** — 경영일지 등 다른 프로젝트 창과 섞이지 않게 한다.

---

## 2.5 ★ 문서를 만들지 마라 — 이 프로젝트가 앓은 병이다

**최근 40커밋 중 문서가 35.4%, 화면이 1.8% 였다.** (진단 §1)
그리고 2026-08-12 총괄은 그 진단을 써놓고 **하루 동안 문서 5개를 더 만들었다.**

```
❌ 새 문서를 만든다        →  규칙이 흩어지고 아무도 다 읽지 않는다
✅ 기존 문서를 고친다      →  규칙 변경은 AGENTS.md 한 곳에만
```

- **분석·설계·계획 문서를 새로 만들지 마라.** 필요하면 카드 본문에 적는다
- **지금 살아 있는 문서는 셋뿐이다** — `AGENTS.md`(일하는 방식) · `CLAUDE.md`(제품) ·
  `docs/design/목업스터디-재구축설계_v1.md`(설계도). **네 번째를 만들지 마라**
- 나머지 `docs/**` 는 전부 **기록**이다. 읽어도 좋지만 규칙으로 삼지 않는다 — §10.1
- 문서를 쓰고 싶으면 먼저 물어라 — **「이걸 안 쓰면 무엇이 안 되나」.**
  답이 «나중에 참고» 면 쓰지 않는다
- **코디네이터의 산출물도 «화면이 나왔나» 로 판정한다.** 문서 개수로 판정하지 않는다

> **목표는 빠른 출시와 유저 데이터를 통한 개선이다. 완벽한 행정이 아니다.**

---

## 3. 완료의 정의 (개정) — 가장 중요

```
전:  코드 + 테스트 + CI
후:  코드 + 테스트 + CI + «그 화면 주소를 열어 눈으로 본 증거»
```

- **화면이 없는 계약·부품 카드는 «완주» 가 아니라 «부품 납품» 이다.**
  반드시 「어느 화면이 이것을 쓰는가」를 후속 카드로 남긴다. **소비자 없는 부품은 만들지 않는다.**
- **화면을 볼 수단이 없는 워커는 그 카드를 끝낼 수 없다.** 코디네이터에게 보고하고,
  코디네이터가 **볼 수 있는 워커에게 넘긴다** — §2.2. «누군가 대신 봐 주기를 기다리는» 자리를 만들지 않는다.
- **「목업 86/86 PASS」를 완주 근거로 쓰지 마라.** 앱을 증명하지 않는다.
  구조 축소는 `node docs/design/qa-app.mjs` 가 숫자로 잡고(§5), **화면 일치는 사람 눈** 으로 판정한다.
- 「촬영 불가」는 **머지 중단 사유다.** 못 찍으면 찍을 수 있는 워커에게 넘긴다.
  2026-08-11 프로덕션 P0 의 직접 원인이 「촬영 불가는 중단 사유가 아니다」 한 줄이었다 — §1.5.

---

## 4. 배정 — Linear 가 정본이다

배정은 **Linear 에서 일어난다.** 채팅으로 프롬프트만 주는 것은 배정이 아니다.
2026-08-12 진단의 결론이 이것이었다 — 배정이 총괄의 채팅창에서 일어나고 Linear 에는 사후 도장만 찍혀서,
**누가 무엇을 들고 있는지 아무도 볼 수 없었다** (§1.4).

```
① 코디네이터가 카드를 세운다        제목 · 완료조건 · 범위를 카드 본문에 적는다
② 카드 하나를 Task 로 만들어 워커에게 붙인다   한 카드 = 한 Task = 한 Dispatch (§2.3)
③ 워커가 착수하면 카드에 착수 코멘트를 남긴다
④ 워커가 끝내면 카드에 완주 도장을 찍고 빠진다
```

**카드 없이 착수하지 않는다.** 카드가 없으면 코디네이터에게 요청한다.

### 4.1 착수·완주 도장 형식

```
착수 — 브랜치 <이름> · base <SHA> · 착수범위 <어디까지>
완주 — PR #n → 머지 SHA · 배포 · health 200 · ★ 화면 확인 증거
```

- **하트비트는 Linear `BBE-94`** 에 코멘트 «추가»(수정 아님).
- 워커가 Linear 에 직접 쓸 수 없으면 완료 보고에 도장 내용을 실어 코디네이터에게 넘긴다.
  **기록 생략은 없다.**

### 4.2 라벨 — 반 라벨은 더 붙이지 않는다

Linear 계정이 하나라 «담당자» 칸으로는 소유를 표현할 수 없다. 그래서 라벨을 써 왔다.

- **기존 반 라벨(`DC-*` · `NC-*` · `DG-*` · `NG-*`)은 새로 붙이지 않는다.**
  §2 에서 반·칸 축을 없앴으므로 가리킬 대상이 없다. **이미 붙은 것은 «그때의 기록» 으로 남긴다** — 떼지 않는다.
- 지금 소유를 표현하는 것은 **카드 상태 + 착수 코멘트 + Orca 의 Dispatch** 다.
- **라벨 체계를 새로 세울지는 미결이다.** 관제판(`tools/board/board.template.html`)이 라벨을 읽고 있어
  바꾸면 그 판도 같이 바뀌어야 한다. **belie 결정 사항** — §10 미결 목록.

---

## 5. 검수

## ★ 검수는 «자기 안에서» 끝낸다 — 다른 창에 넘기지 않는다

```
서브에이전트 자체 검수      워커가 «자기 서브에이전트» 를 띄워 검수한다
                          belie 손이 0 이다 — 다른 창에 붙여넣을 일이 없다
PASS 면 → 자기가 머지한다   승인을 기다리지 않는다
```

**다른 창에 검수를 넘기는 순간 belie 가 그 창에 붙여넣어야 한다.**
2026-08-12 에 그렇게 설계했다가 **belie 가 병목이 됐다.** 짝 반장 검수·2단 검수 모두 폐지했다(아래).

**빠른 출시가 목표다. 완벽한 검수 절차가 목표가 아니다.**
문제가 생기면 **revert 가 복구 수단**이다 — 사전 승인이 아니라 사후 되돌리기로 안전을 확보한다.

### 대신 «사람이 아닌 것» 이 막는다

```
bash scripts/check.sh          lint · typecheck · test          ← 훅과 CI 가 강제
node docs/design/qa-app.mjs    구조 축소 (D73)                  ← 숫자로 잡는다
화면 확인 증거                  1440px · 375px 스크린샷           ← 워커가 스스로 찍는다
실행 잠금 4개                   provider · 자격증명 · 058 · 발송  ← belie 스위치. 검수와 무관
```

### 안전망은 결과로 둔다

```
같은 카드가 2회 반려·되돌림  →  belie 에게 올린다. 설계가 틀렸다는 뜻이다
프로덕션이 깨졌다            →  즉시 revert. 원인은 그다음에 찾는다
```

### 2단 검수는 폐지했다 (2026-08-12) — 사람 대신 «잠금» 이 막는다

전에는 돈·마이그레이션·D24 권한·구조 넷을 **반대편 기계의 반장**이 추가로 봤다. 폐지했다.

| 왜 폐지했나 | |
| --- | --- |
| 실제로 잡은 결함 | **0건.** 잡힌 4건은 전부 «자기» 또는 «옆 세션» 이 잡았다 |
| 비용 | 기기 교차라 **belie 가 다른 컴퓨터를 켜야** 검수가 돈다 — belie 가 병목이 된다 |
| 대체재가 생겼다 | `qa-app.mjs`(BBE-140)가 구조 축소를 **숫자로** 잡는다. 사람 눈보다 정확하다 |

> **2026-08-20 추가 폐기 — 「같은 물리 기기 레인에서 닫는다」.**
> 「데탑에서 시작한 것은 데탑에서 검수하고 다른 기기의 검수자를 기다리지 않는다」는 규칙이었다.
> **기기 축이 없어졌으므로(§2.2) 기다릴 «다른 기기의 검수자» 자체가 없다.**
> 검수는 워커가 자기 안에서 끝내고, 그 결과를 코디네이터가 받는다.

**대신 이 넷이 막는다 — 읽어서 막는 게 아니라 «못 하게» 만든다.**

```
구조 축소       node docs/design/qa-app.mjs
               착수 전/후 숫자를 비교한다. 컬럼·아이템·이동규칙이 줄면 FAIL (D73)

돈             실행 잠금 4개 — 전부 belie 개별 승인이다
               provider 발송 설정 · worker 자격증명 · 058 hosted 적용 · 실제 발송 1건
               ★ BBE-30 이 증거다 — 머지가 돼버렸는데도 돈이 안 나갔다. 스위치가 꺼져 있었다

마이그레이션    hosted 적용이 belie 승인 사항
               + 「두 번 실행해도 안전」을 로컬/PGlite 로 기계 증명

D24 권한       실행형 테스트 필수 — 조회 범위 밖 건이 «안 보이는지» 코드로 증명
               말로 «지켰다» 는 근거가 아니다
```

**★ 검수는 자기 폴더가 아니라 «원격에서 받은 것» 으로 본다.**

```bash
gh pr checkout <PR 번호>          # 권장 — 브랜치 이름표까지 정확히 맞춘다
```

2026-08-11 에 「정본 파일이 원격에 없음」으로 6세션이 몇 시간 멈췄다. 이 한 줄이 그것을 막는다.

> **★ 워크트리에서는 `git fetch origin <브랜치>` 로 끝내지 마라.**
> 이 저장소의 fetch refspec 은 `main` 에만 좁혀져 있다(`+refs/heads/main:refs/remotes/origin/main`).
> 다른 브랜치를 fetch 하면 `FETCH_HEAD` 만 갱신되고 `origin/<브랜치>` 이름표는 안 움직인다 —
> **검수자가 낡은 커밋을 보고 「저장소에 없다」고 오판한다.** 실제로 그렇게 판정이 두 번 뒤집혔다.

**★ Linear·GitHub 댓글은 워커 호출 수단이 아니다.**
댓글은 «도는 창» 을 깨우지 못한다. 코디네이터가 **Dispatch 로 직접** 호출한다.
불합격이면 같은 터미널에 새 Dispatch 를 붙인다(§2.3). 댓글만 남기고 기다리면 아무도 오지 않는다.

**서브에이전트 검수 절차와 프롬프트: `docs/playbooks/subagent-review.md`**
핵심 조건 — **서브에이전트에 부모의 결론·변명을 한 줄도 넣지 않는다.** 유도하면 그 검수는 무효다.
**자기 PR 을 자기가 approve 하지 않는다.**

---

## 6. 마비 대비

### ① 상태를 세션 머리에서 파일로 뺀다 — 근본

| 무엇 | 어디에 | 소유 |
| --- | --- | --- |
| 배정 · 진행 | **Linear** (카드 상태 + 착수/완주 도장) | 코디네이터가 세우고, 워커가 도장 |
| 판정 | `docs/handoff/결정대장.md` D번호 | 코디네이터 |
| 진행 중인 작업 | Orca 의 Task · Dispatch | Orca 가 자동 기록 |

**세션 문맥은 언제든 날아간다.** 2026-08-11 에 실제로 한 번 날아갔다.
**파일이나 Linear 에 없는 것은 존재하지 않는 것으로 취급한다.**

### ② 코디네이터가 마비되면

코디네이터는 하나다. 대행을 미리 지정하지 않는다 — 지정해 두면 그 세션도 문맥을 유지해야 해서 비용이 든다.
대신 **언제든 새 코디네이터를 세울 수 있게** 상태를 밖에 둔다.

```
새 코디네이터가 이어받는 법
  ① Linear 를 연다 — 무엇이 진행 중이고 무엇이 막혀 있는지가 거기 있다
  ② orca orchestration run-list / task-list — 살아 있는 Task 와 Dispatch 를 본다
  ③ orca orchestration run-use --id <run> — 그 Run 을 자기 터미널에 묶는다
  ④ 돌고 있는 워커는 «그대로 둔다». 다시 세우지 않는다 — 하던 일이 날아간다
```

**새 결정이 필요하면 belie 에게 올린다.** 이어받은 코디네이터가 판단까지 새로 하면 그것이 중복의 시작이다.

### ③ 워커가 마비되면

- **돌고 있는지 먼저 확인한다.** 하트비트나 화면 활동이 있으면 «살아 있는 것» 이지 «끝난 것» 이 아니다.
  코딩 작업은 15~60분이 정상이다. 조용하다고 끄지 않는다.
- 정말 죽었으면 **같은 카드로 대체 워커를 세운다.** 이때 배치(기계·워크트리·엔진)를 다시 지정한다 —
  자동으로 물려받지 않는다.
- **워커가 안고 있던 카드는 반드시 판에 되돌린다.** 안고 죽으면 그 카드는 아무에게도 안 보인다(§8.2).

### ④ 잔량 규칙

**한도가 가까워지면 «멈추기 전에» 인수인계 도장을 남긴다.**

```
지금 무엇을 하던 중 · 다음 순서 · 미결 판단        + 브랜치명과 head SHA
```

---

## 7. 실행 체인 (순서 고정)

```
① git fetch → 최신 origin/main 위 rebase        충돌 시 임의 해결 금지 · 중단 보고
② 내 카드가 있는지 확인                          없으면 착수 금지 (§4)
③ bash scripts/check.sh 초록
④ 서브에이전트 자체 검수                         docs/playbooks/subagent-review.md
⑤ push → PR(본문에 카드번호) → CI 초록
⑥ ★ 화면 확인 — 그 주소를 열어 본 증거            못 보면 볼 수 있는 워커에게 넘긴다 (§2.2)
⑦ squash merge → 배포 → health 200
⑧ Linear 완주 도장 + 코디네이터에게 완료 보고
```

**⑥ 이 없으면 완주가 아니다.**

## 8. 자율 완주 정책 — 승인 대기로 멈추지 않는다

> **「자율 완주」는 «belie 승인 없이 머지·배포까지 간다» 는 뜻이다.**
> «워커가 다음 카드를 스스로 집는다» 는 뜻이 **아니다** — 그건 2026-08-20 에 코디네이터 소유로 옮겼다(§2.1).
> 워커가 스스로 다음 일을 시작하면 코디네이터가 무엇이 도는지 알 수 없고, 중복이 다시 시작된다.

- **머지와 배포까지 belie 승인 없이 진행한다.** 「승인 대기」로 멈추지 않는다.
- belie 에게 올라가는 것은 **4가지뿐**이다. 그 판단은 코디네이터가 한다:
  ① 고객 실데이터 비가역 변경 ② 돈·보안 ③ 정책 방향 전환 ④ 외부 발행
- 문제가 생기면 **revert 가 복구 수단**이다. 사전 허락이 아니라 사후 되돌리기로 안전을 확보한다.
- 같은 승인 게이트가 반복되면 skill issue 가 아니라 **harness issue** — 이 문서를 고친다.

### 8.1 〔기록〕 왜 이 절이 생겼나

세션들이 멈춰 있던 자리는 세 곳이고, **전부 «깨울 방법이 없는» 자리**였다.

```
"나는 끝났다"                 → 다음 일을 아무도 주지 않는다
"검수 결과가 나와야 진행"       → 검수자가 안 붙으면 무한 대기
"다른 세션이 마무리해줘야 진행"  → 그쪽이 끝나도 «풀렸다» 를 아무도 알려주지 않는다
```

**이건 게으름이 아니라 구조 결함이었다.** 차단된 세션은 차단이 풀린 것을 알 수단이 없었다.
Linear 댓글도 알림도 «도는 창» 을 깨우지 못한다.

> **셋 중 둘은 이제 구조가 푼다.**
> 첫째 — 코디네이터가 완료 보고를 받는 즉시 다음 Dispatch 를 붙인다. 「아무도 안 준다」가 없다.
> 둘째 — 검수를 자기 안에서 끝내므로(§5) 기다릴 검수자가 없다.
> **셋째(남이 해줘야 함)만 남는다.** 그것이 §8.2 의 반납이다.

### 8.2 워커가 멈추고 싶을 때 — 기다리지 말고 반납한다

| 멈추고 싶을 때 | 대신 이렇게 |
| --- | --- |
| **"끝났다"** | **완료 보고를 보내고 그 턴을 끝낸다.** 다음 카드는 코디네이터가 준다 |
| **"검수 대기"** | 검수는 자기 안에서 끝낸다(§5). 기다릴 검수자가 없다 |
| **"남이 해줘야 함"** | ★ **카드를 반납한다** — 기다리지 않는다 |

**★ 차단은 «기다림» 이 아니라 «반납» 이다.**

```
반납 절차
  ① 카드 상태를 Blocked 로 바꾸고, 막는 것을 «카드번호» 로 적는다
        blocked-by: BBE-16 (PR #141)      ← «부를 수 있는 것» 을 적는다
  ② 도장에 적는다
        막는 것:   BBE-16 · PR #141
        겹친 것:   app/src/app/(app)/deals/[dealId]/page.tsx · actions.ts
        재개 조건: PR #141 머지
        현재까지:  브랜치 codex/bbe-19-r2 · head 1eed7c3 · focused 6/6 PASS
  ③ 완료 보고를 «실패» 로 보내고 무엇이 막았는지 적는다 — 코디네이터가 해제를 감시한다
```

> **★ 「PR #141 때문에 못 한다」로 끝내지 마라. 무엇이 막는지를 «카드번호» 로 적어라.**
> 카드번호가 있으면 코디네이터가 그 카드의 Dispatch 를 찾아 **직접 호출**할 수 있다.
> 번호 없이 「어떤 PR 때문에」라고만 적으면 코디네이터가 소유자를 처음부터 다시 찾아야 하고,
> 그 한 단계가 또 지연이 된다.
>
> **막는 것의 카드번호를 모르면 — 그 작업에 카드가 없다는 뜻이다(§4 위반).**
> 「카드 없음」이라고 적고 코디네이터에게 올린다. **차단 보고가 카드 누락을 자동으로 잡아내는 구조다.**

반납된 카드는 코디네이터의 **«해제 감시» 목록**에 들어간다. 조건이 풀리면 코디네이터가 재배정한다.
**워커가 카드를 안고 기다리면 그 카드는 아무에게도 안 보인다. 반납해야 판에 뜬다.**

### 8.3 완주할 때의 의무 — 도장으로 끝이 아니다

```
① 내가 «막고 있던» 카드가 있는지 확인한다   blocked-by 에 내 카드번호가 걸린 것
② 있으면 완료 보고에 «해제됨» 을 적는다      코디네이터가 그 워커를 직접 호출한다
③ 완료 보고를 보내고 그 턴을 끝낸다         ★ 스스로 다음 일을 시작하지 않는다
```

②를 빠뜨리면 내가 푼 차단이 계속 남는다. **푼 사람이 알리지 않으면 아무도 모른다.**

### 8.4 코디네이터의 라운드 종료 의무 — 가장 자주 빠지는 곳

**한 라운드를 돌렸으면 «끝났다» 로 멈추지 않는다.** 반드시 아래를 하고 belie 에게 **역제안**한다.

```
① 판 조회 — Linear 와 Orca 의 Task·Dispatch 에서 지금 무엇이 어디서 멈춰 있나
② 해제 판정 — 선결조건이 풀린 반납 카드를 찾아 즉시 재배정 (새 Dispatch 로 «직접» 호출)
③ 정산 — 끝난 워커의 터미널을 회수한다. 안 하면 목록이 길어져 belie 가 판을 못 읽는다
④ 병목 1건 지목 — 지금 가장 오래 멈춰 있는 것과 그 원인
⑤ 역제안 — 「다음은 이것을 하겠습니다. belie 손이 필요한 것은 X 하나입니다」
```

> **belie 에게 «무엇을 할까요?» 라고 묻지 않는다.**
> **«이렇게 하겠습니다 · 당신 손이 필요한 건 이것뿐입니다» 라고 제안한다.**
>
> belie 손이 필요한 것은 원래 셋뿐이다 — **계정·비밀값 · 승인 판단 · 눈으로 보는 확인.**
> 그 밖의 것을 물으면 그것은 코디네이터가 판단을 미룬 것이다.

### 8.5 대기 도장 형식

멈추는 것 자체는 괜찮다. **보이지 않게 멈추는 것이 문제다.**

```
대기 — BBE-19 담당 · 2026-08-12 14:00
  막는 것:   BBE-16 · PR #141
  겹친 것:   app/src/app/(app)/deals/[dealId]/page.tsx · actions.ts
  재개 조건: PR #141 머지
  현재까지:  브랜치 codex/bbe-19-r2 · head 1eed7c3 · focused 6/6 PASS
  → 카드 반납 완료. 나는 여기서 턴을 끝낸다.
```

**두 칸이 비면 그 대기는 영원하다 — «막는 것의 카드번호» 와 «재개 조건».** 반드시 채운다.

---

## 9. 안전 규칙

### 9.1 Git · 저장소

- 공유 checkout 에서 작업하지 않는다. 최신 `origin/main` 에서 만든 **전용 worktree** 에서 한다.
- 한 파일에 활성 writer 는 한 명. 남의 미커밋 파일을 수정·stage·삭제하지 않는다.
- 남의 브랜치를 checkout·rebase·force-push·reset·delete 하지 않는다.
- **Supabase 스키마는 새 migration 파일로만 추가.** 기존 파일 수정 금지.
  번호는 **머지 직전에 `origin/main` 최신 +1 로 재확정**한다. 선점 금지.
- 커밋 전 `bash scripts/check.sh` 통과. `.githooks/pre-commit` 이 같은 것을 돌린다.
- **PR 충돌이 나면 십중팔구 `docs/worklog.md` 다.** 코드 충돌보다 이 한 파일이 압도적으로 많다.
  붙기 전에 `git merge-tree` 로 먼저 확인한다.

### 9.2 비밀값

**`CLAUDE.md` 「절대 금지 2가지」가 정본이다.** 여기서는 실무만 적는다.

- 값을 **출력·문서화·커밋하지 않는다.** 설정 여부와 변수명만 기록한다.
- 검수 로그·스크린샷·터미널 캡처에도 값이 실리지 않게 한다.

### 9.3 제품

**`CLAUDE.md` 「제품」 절이 정본이다.** 제품 어휘·빈 상태·뷰포트·브랜드 토큰은 전부 거기 있다.

여기서 한 번 더 못 박는 것은 이것 하나다 —
**«비우기» 지 «지우기» 가 아니다. 구조가 줄어든 PR 은 무조건 FAIL** (D71~D75).
`node docs/design/qa-app.mjs` 로 착수 전/후 숫자를 비교한다(§5).

### 9.4 목업 읽기 — 브라우저 없이

```bash
node docs/design/dump-mockup.mjs          # 전체
node docs/design/dump-mockup.mjs new      # 탭 하나 (new/contact/work/notice)
```

`file://` 로 목업을 열려고 하지 마라. 막히면 **우회하지 말고** 위 명령을 쓴다.
브라우저가 되는 워커는 열어봐도 좋다. 다만 **정본 판정은 위 명령의 출력**이다.

### 9.5 기록

- 착수·완주 도장은 **Linear** 에 남긴다. 형식은 §4.1.
- `docs/worklog.md` 는 append-only. 기존 기록을 수정·삭제하지 않는다.
- **`docs/` 에 새 문서를 만들지 않는다** (§2.5). 규칙은 이 파일, 진행은 Linear 카드.

---

## 10. 폐기 — 이 목록이 있어야 과거 문서에 안 끌려간다

**아래는 더 이상 유효하지 않다. 과거 문서가 요구하더라도 따르지 않는다.**

### 10.1 2026-08-20 일원화에서 폐기한 것

| 폐기 대상 | 왜 |
| --- | --- |
| **「4반 × 6세션 = 24」 · `DC`/`NC`/`DG`/`NG` 반 이름** | 기계·진영·칸 세 축은 «사람이 손으로 관리해야 했기 때문에» 생긴 것이다. 관리를 Orca 가 맡으면서 이유가 사라졌다 — §2.2 가 대체 |
| **반장(`01`) 관련 잔여 문구 전부** | 2026-08-12 에 폐지했는데 **같은 커밋의 다른 파일에도, 같은 파일 다른 절에도 남아 있었다.** 「01 은 코드를 만들지 않는다」·「01 반장 겸 검수자」 전부 무효 |
| 「짝 반장의 독립 검수」 · 「같은 물리 기기 레인에서 닫는다」 | 기기 축이 없어져 **기다릴 상대 자체가 없다** — §5 |
| 「총괄은 git commit·push 를 할 수 없다」 · 「05 칸이 저장소 쓰기를 겸한다」 | 그건 GUI 총괄의 제약이었다. 코디네이터는 터미널 세션이라 직접 올린다 — §2.1 |
| **「`배정판.md` 가 정본 · 여기 없는 카드는 착수 금지」** | 배정의 정본은 **Linear** 다 — §4. 「착수 금지」가 두 곳에 걸려 규칙이 서로를 막고 있었다 |
| 「워커가 끝나면 다음 착수 가능 카드를 스스로 집는다」 | 코디네이터가 준다 — §8.2. 스스로 집으면 무엇이 도는지 아무도 모르고 중복이 다시 시작된다 |
| `docs/playbooks/worker-onboarding.md` **구판** | 스스로를 「모아워크 규칙 정본」이라 부르면서 **이 §10 이 이미 폐기한 것들을 워커에게 지시**하고 있었다. 전면 교체 |
| 그 구판이 지시하던 「375/390px 검증 면제」 · 「촬영 불가는 머지 중단 사유가 아니다」 | 아래 10.2 에서 이미 폐기된 것. **워커가 읽는 문서에는 계속 살아 있었다** |
| 그 구판의 `CT01~10`/`GT` 세션명 · `docs/plans/naming-rules.md` · `onboarding-moawork.md` 참조 | 세 파일 다 **저장소에 없다.** 없는 문서를 읽으라고 지시하고 있었다 |
| 「`docs/coordination/**` 는 디스패치(MWC) 단독 소유」 | MWC 라는 자리가 없다 |
| `docs/coordination/**` · `docs/plans/README.md` 의 **「정본」 자칭 전부** | 기록으로 강등했다. 각 파일 머리에 배너를 달았다 — 지우지는 않았다 |

### 10.2 2026-08-12 개정에서 폐기한 것 〔유지〕

| 폐기 대상 | 왜 |
| --- | --- |
| 「촬영 불가는 머지 중단 사유가 아니다」 | **2026-08-11 P0 의 직접 원인.** §3 이 대체 |
| 「375/390px 검증 면제」 | BBE-23(모바일 QA)이 살아 있다. `CLAUDE.md` 「제품」이 대체 |
| 하트비트 `BBE-75` | 오기. 정본은 **`BBE-94`** — §4.1 |
| 세션명 `CT01~10`·`GT01~10`·`G작업반장`·`MW01~10` | 폐기 |
| 「마이그레이션 최신 030, 코덱스는 032부터」 | 현재 **097**. §9.1 의 «머지 직전 최신+1» 로 대체 |
| 「공급자 레인 불가침 — 코덱스 구역 카드 BBE-26·27·28·29·30·31」 | 해당 카드 대부분 완료. §2·§4 로 대체 |
| `docs/plans/배정-260810/` (42파일) · `배정-V6-*.md` · `slot-board.md` · `capacity-plan-*` | 40칸 배정. 편제 개편으로 폐기 |
| `docs/plans/DISPATCH-QUEUE-*` · `ROUTING-R1/R2-*` | 구 디스패치 |
| `session-registry.yaml`·`dispatch-queue.yaml`·`provider-status.yaml` | SYNC-R1(2026-07-22)에서 이미 폐기 |
| `tools/dashboard.html` · `docs/plans/dashboard.html` | 중복본. 정본은 `tools/board/board.template.html` |

### 10.3 미결 — belie 결정 대기

**폐기하지 않았다. 결정이 필요해서 남겨 둔 것이다.**

| 미결 | 무엇이 걸리나 |
| --- | --- |
| **라벨 체계를 새로 세울까** | 관제판(`tools/board/board.template.html`)이 반 라벨을 읽고 있다. 바꾸면 그 판도 같이 바뀌어야 한다 — §4.2 |
| **`docs/worklog.md` 를 쪼갤까** | 266 KB 한 파일이라 워커 둘이 동시에 쓰면 **반드시** 충돌한다. 쪼개면 사라지지만 append-only 규칙을 바꾸는 일이다 — §9.1 |
| **끝난 SYNC 라운드를 archive 로 옮길까** | `ROUND-1`~`34`. 지우는 게 아니라 옮기는 것 |
| **죽은 터미널·쌓인 워크트리 정리** | 되돌릴 수 없어서 승인 없이는 손대지 않는다 |
| **`supabase/migrations/` 에 `014` 번호가 둘이다** | `014_platform_metrics_daily.sql` · `014_reserve_crm_route_slugs.sql`. F3 이 경고하던 바로 그 상황인데 이미 적용됐을 수 있다. 카드로 세울지 판단 필요 |

---

## 11. 개발 명령

```bash
npm install                          # node_modules 가 비면 lint 가 «eslint 없음» 으로 실패한다
npm run dev
bash scripts/check.sh                # lint + typecheck + test — 이것이 게이트다
node docs/design/qa-app.mjs          # 구조 축소 검사 (D73)
node docs/design/qa-mockup.mjs       # 목업 자기검사 (앱은 검사하지 않는다 — §1.2)
node docs/design/dump-mockup.mjs     # 목업을 글로 읽기
```

---

## 12. 개정 이력

| 날짜 | 내용 |
| --- | --- |
| 2026-08-20 | **일원화.** 정본을 `CLAUDE.md`·`AGENTS.md` 둘로 확정 · 편제를 «코디네이터 1 + 워커 N» 으로 교체 · 반/칸/진영 축 폐지 · 워커 온보딩 전면 교체 · `docs/**` 기록으로 강등 · §0 신설 |
| 2026-08-12 | 전면 개정. 종합 진단 반영 · 40칸→24세션 · 완료의 정의 개정 · 라벨 배정 · 3중 방어 |
| 2026-08-09 | 운영 규약 (폐기 — §10.2) |
