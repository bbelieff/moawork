# AGENTS.md — 모아워크 작업 지침 (정본)

**2026-08-12 전면 개정.** 이 문서가 모든 세션의 근본 지침이다. 세션 시작 시 이것부터 읽는다.
이전 판(2026-08-09 운영 규약)은 폐기됐다 — §10 에 무엇이 왜 폐기됐는지 적었다.

읽는 순서: **`AGENTS.md`(이 문서) → `docs/coordination/배정판.md` → `CLAUDE.md` → 배정된 카드**

---

## 1. 왜 개정하는가 — 2026-08-12 종합 진단

`origin/main` 최근 40커밋을 실측한 결과다. 인상이 아니다.
전문: `docs/coordination/진단-2026-08-12.md`

### 1.1 만든 것의 1.8% 만 화면이다

추가된 29,688줄의 행선지:

```
 10,499줄  35.4%  문서 · 설계          ← 화면의 20배
  5,994줄  20.2%  테스트
  5,453줄  18.4%  DB 마이그레이션
  4,837줄  16.3%  계약 · 서버 로직
  1,339줄   4.5%  화면 부품
  ────────────────────────────
    524줄   1.8%  ★ 사용자가 보는 화면
```

**세션 잘못이 아니다.** 프롬프트가 「계약을 만들어라·테스트로 증명해라」였고 전부 그렇게 했다.
**「화면에 나오게 해라」가 완주 조건에 없었다.** 그것이 총괄의 잘못이다.

### 1.2 앱이 목업과 같은지 검사하는 것이 0건이다

```
qa-mockup.mjs 가 읽는 것   →  목업 HTML 자기 자신
앱을 읽는 부분              →  없음
scripts/check.sh 에서 실행  →  0회 (아예 안 돈다)
```

**즉 「목업 86/86 PASS」는 «목업 파일이 목업답다» 만 증명해 왔다.**
모든 세션이 이것을 착수·완주 근거로 적었다. **전원이 성실하게, 아무것도 증명하지 않았다.**

구조도 어긋나 있다 — 목업은 **탭 6개짜리 한 화면**인데 앱은 **주소 25개**다.

### 1.3 파일 52개가 어느 화면에도 닿지 않는다

화면 진입점 78개에서 import 를 따라간 결과, 306개는 닿고 **52개는 안 닿는다**(전체의 15%).
`lib/accounting` 5 · `lib/messaging` 4 · `lib/policyfund` 4 …

세션들은 알고 있었고 정직하게 보고했다:

> *"현재 main 에서 `DealLedgerPanel`·`LedgerExportButton` 을 소비하는 화면 import 는 **0**입니다.
> 실제 업무 상세 화면 노출을 주장할 수 없습니다."* — 2026-08-11

**보고는 완벽했다. 받아서 조치할 사람이 없었을 뿐이다.**

### 1.4 중복 8건 — Linear 가 막지 못한 이유

카드 담당자를 실측하니 **In Progress 16건이 전부 «카뮈» 한 사람**이었다.
44개 세션이 Linear 계정 하나를 공유해서, **누가 무엇을 들고 있는지 표현할 방법이 없었다.**

세션이 착수 전에 카드를 열면 `In Progress · 담당 카뮈` 만 보인다.
그게 나인지 옆 세션인지 알 길이 없으니 두 번째 세션이 착수해 버린다.

**더 근본적으로는 — 배정이 Linear 에서 일어나지 않았다.** 총괄의 채팅창에서 일어났고
Linear 에는 사후 도장만 찍혔다. **Linear 는 기록소였지 배정 도구가 아니었다.**

### 1.5 회귀는 「화면 확인 없이 머지」의 필연이었다

2026-08-11 P0 (오너가 자기 제품에 못 들어감):

```
CI ✅ · 단위 테스트 451줄 ✅ · 독립 검수 ✅ · 화면 확인 ❌ NOT_RUN
→ 사장이 제품에 못 들어감
```

바뀐 것은 **막히는 조건이 아니라 「막혔을 때 어디로 보내는가」** 였다.
**단위 테스트로는 절대 안 잡힌다.** 각 함수는 설계대로 정확히 동작한다. 사람이 열어봐야 보인다.

그리고 그것을 건너뛰어도 되게 만든 것은 구 AGENTS.md §6.3 의
**「촬영 불가는 머지 중단 사유가 아니다」** 한 줄이다. 그 줄은 §10 에서 폐기한다.

---

## 2. 편제 — 4반 × 6세션 = 24

기계 2 × 진영 2 = 반 4개. **C = 클로드 · G = 지피티(코덱스)**

| 반 | 뜻 | 성격 | 근거 |
| --- | --- | --- | --- |
| **DC** | 데탑 클로드 | 조립(화면) | 브라우저가 있다 → 화면을 눈으로 볼 수 있다 |
| **NC** | 노트북 클로드 | 조립·회수 | 〃 |
| **DG** | 데탑 코덱스 | 부품(DB·계약) | 브라우저가 없다 → 눈이 필요 없는 일 |
| **NG** | 노트북 코덱스 | 관문·검증 | 〃 |

```
반 하나의 속 — 6세션
  총괄     문맥 유지 · 배정 집행 · 도장 취합 · 인수인계     ← 판단하지 않는다
  01       반장 겸 검수자                                   ← 코드를 만들지 않는다
  02~05    작업원 4칸                                       ← 만든다
```

**세션명**

```
DC-03 · NG-01 · DG-05 …        기계(D/N) + 진영(C/G) + 번호
서명:  [DC-03]                  카드 제목에는 프로젝트 병기 — BBE-44(MoaWork)
```

> **원칙 — 화면(`page.tsx`·`layout.tsx`)은 C 진영만 만진다.**
> 2026-08-11 프로덕션 P0(PR #160)은 코덱스 브랜치였다. 화면을 바꿨는데 볼 수단이 없었다.
> G 진영이 화면을 바꿔야 하면 **DC/NC 에 넘기거나 MWC 에 클릭스루를 의뢰**한다.

**칸별 소유 영역과 카드는 `docs/coordination/배정판.md` 가 정본이다. 여기 없는 카드는 착수 금지.**

---

## 3. 완료의 정의 (개정) — 가장 중요

```
전:  코드 + 테스트 + CI
후:  코드 + 테스트 + CI + «그 화면 주소를 열어 눈으로 본 증거»
```

- **화면이 없는 계약·부품 카드는 «완주» 가 아니라 «부품 납품» 이다.**
  반드시 「어느 화면이 이것을 쓰는가」를 후속 카드로 남긴다. **소비자 없는 부품은 만들지 않는다.**
- 브라우저가 없는 G 진영은 **MWC 에 클릭스루를 의뢰**하고 그 확인을 받아야 완주다.
- **「목업 86/86 PASS」를 완주 근거로 쓰지 마라.** 앱을 증명하지 않는다.
  `qa-app.mjs`(NG-02)가 나오기 전까지 화면 일치는 **사람 눈** 으로만 판정한다.
- 「촬영 불가」는 이제 **머지 중단 사유다.** 못 찍으면 찍을 수 있는 세션에 넘긴다.

---

## 4. 배정 — 라벨이 곧 배정이다

Linear 계정이 하나라 «담당자» 칸은 쓸 수 없다. **라벨을 쓴다.**

```
BBE-122  [DG-04]      ← 이 카드는 데탑코덱스 04번 소유
BBE-31   [NC-02]
```

```
① 배정은 «라벨을 붙이는 행위» 다. 채팅으로 프롬프트만 주는 것은 배정이 아니다
② 라벨 없는 카드는 착수 금지. 라벨이 없으면 자기 반 총괄에게 요청한다
③ 라벨이 둘 이상이면 중복이다 → 총괄이 하나로 정리한다. 관제판이 이것을 경고한다
④ 한 카드 = 한 칸. 옮기려면 앞의 라벨을 «떼고» 옮긴다. 동시 보유 금지
```

관제판(`tools/board/board.template.html`)은 이미 라벨을 읽고 있다. 붙이기만 하면 판에 보인다.

---

## 5. 검수

```
평시   DC ↔ DG      NC ↔ NG      같은 기계 안에서 진영을 건넌다
                                 (다른 모델이 봐야 «같은 쪽으로 잘못 읽기» 가 걸린다)

2단    돈 · 마이그레이션 · D24 권한 · 프리셋/컬럼/아이템 «구조»
       → 반대편 기계 반장이 추가로 본다.  DC→NG · DG→NC · NC→DG · NG→DC

비상   진영이 통째로 마비(토큰 소진)되면  DC ↔ NC · DG ↔ NG
       같은 진영이라 열등하다. 도장에 「비상검수 — 동일 진영」을 반드시 적는다
       → 진영이 살아나면 그 목록이 재검수 대상이 된다
```

**★ 검수자는 자기 폴더가 아니라 원격에서 받은 것으로 본다.**

```bash
git fetch origin && git checkout <PR head SHA>
```

2026-08-11 에 「정본 파일이 원격에 없음」으로 6세션이 몇 시간 멈췄다. 이 한 줄이 그것을 막는다.

**★ 01(반장)은 코드를 만들지 않는다.** 만들게 하면 검수가 멈춘다 —
08-11 관문 3건이 「검수자가 안 붙어 21:47 이후 정지」한 원인이 그것이다.

**★ 같은 물리 기기 레인에서 닫는다.** *(2026-08-11 세션 제안 · 채택)*
데탑에서 시작한 작업은 데탑 안에서 검수하고, 그 결과를 소비하는 후속 작업도 데탑에 배정한다.
노트북도 같다. **다른 기기의 검수자를 기다리지 않는다.**
지정 검수자가 60분 안에 응답하지 않으면 총괄이 **같은 기기의 다른 유휴 세션**으로 즉시 재배차하고,
도장에 「지정 검수자 무응답 60분 → 보조 검수 <세션명>」을 남긴다.
단 위 **2단(돈·마이그레이션·권한·구조)은 이 규칙에서 제외** — 그것만 반대편 기기가 본다.

**★ Linear·GitHub 댓글은 세션 호출 수단이 아니다.** *(2026-08-11 세션 제안 · 채택)*
총괄이 검수 세션을 **직접 호출**하고, 판정 ID를 확인한 뒤 작성자 세션을 **직접 다시 호출**해
수정 또는 머지·배포 체인을 잇는다. 댓글만 남기고 기다리면 아무도 오지 않는다.

**1단 자체 검수(전 PR 필수)** 절차와 서브에이전트 프롬프트: `docs/playbooks/subagent-review.md`
핵심 조건 — **서브에이전트에 부모의 결론·변명을 한 줄도 넣지 않는다.** 유도하면 그 검수는 무효다.
**자기 PR 을 자기가 approve 하지 않는다.**

---

## 6. 마비 대비 (3중 방어)

### ① 상태를 세션 머리에서 파일로 뺀다 — 근본

| 무엇 | 어디에 | 소유 |
| --- | --- | --- |
| 배정 | `docs/coordination/배정판.md` + Linear 라벨 | C 총괄(데탑) |
| 판정 | `docs/handoff/결정대장.md` D번호 | C 총괄(데탑) |
| 진행 | Linear `BBE-94` 도장 + 관제판 | 각 반 총괄 |

**총괄 세션 문맥은 언제든 날아간다.** 2026-08-11 에 실제로 한 번 날아갔다.
파일에 없는 것은 존재하지 않는 것으로 취급한다.

### ② 대행 — 지정하되 권한을 좁힌다

```
C 총괄 마비  →  ① DC-01      ② C 진영 전체 마비면 G 총괄
G 총괄 마비  →  ① DG-01      ② G 진영 전체 마비면 C 총괄
```

| 대행이 하는 것 | 못 하는 것 |
| --- | --- |
| 배정판대로 배정 집행 · 라벨 부착 | 새 D번호 등재 |
| 도장 확인 · 1단 통과분 머지 | 목업 수정 |
| 회귀 감시 · 관제판 갱신 | 편제 변경 · 카드 신설 · 배정 변경 |

**새 결정이 필요하면 belie 에게 올린다.** 대행이 판단까지 하면 그것이 중복의 시작이다.

### ③ 잔량 규칙

**총괄과 반장은 한도가 가까워지면 «멈추기 전에» 인수인계 도장을 남긴다.**

```
지금 무엇을 하던 중 · 다음 순서 · 미결 판단        + 브랜치명과 head SHA
```

---

## 7. 실행 체인 (순서 고정)

```
① git fetch → 최신 origin/main 위 rebase        충돌 시 임의 해결 금지 · 중단 보고
② 카드에 내 라벨이 붙어 있는지 확인               없으면 착수 금지
③ bash scripts/check.sh 초록
④ 1단 자체 검수 (서브에이전트)                    docs/playbooks/subagent-review.md
⑤ push → PR(본문에 카드번호) → CI 초록
⑥ 짝 반장의 독립 검수 PASS                        원격에서 받아서 본다
⑦ ★ 화면 확인 — 그 주소를 열어 본 증거            C진영은 직접, G진영은 MWC 의뢰
⑧ squash merge → 배포 → health 200
⑨ Linear 완주 도장 + 라벨 유지
```

⑦ 이 없으면 완주가 아니다. **⑦ 을 못 하면 할 수 있는 세션에 넘긴다.**

---

## 8. 자율 완주 정책

- **머지와 배포까지 belie 승인 없이 진행한다.** "승인 대기"로 멈추지 않는다.
- belie 에게 올리는 것은 **4가지뿐**:
  ① 고객 실데이터 비가역 변경 ② 돈·보안 ③ 정책 방향 전환 ④ 외부 발행
- 문제가 생기면 **revert 가 복구 수단**이다. 사전 허락이 아니라 사후 되돌리기로 안전을 확보한다.
- 같은 승인 게이트가 반복되면 skill issue 가 아니라 **harness issue** — 이 문서를 고친다.

---

## 9. 안전과 제품 규칙

### 9.1 Git · 저장소

- 공유 checkout 에서 작업하지 않는다. 최신 `origin/main` 에서 만든 **전용 worktree** 에서 한다.
- 한 파일에 활성 writer 는 한 명. 남의 미커밋 파일을 수정·stage·삭제하지 않는다.
- 남의 브랜치를 checkout·rebase·force-push·reset·delete 하지 않는다.
- **Supabase 스키마는 새 migration 파일로만 추가.** 기존 파일 수정 금지.
  번호는 **머지 직전에 `origin/main` 최신 +1 로 재확정**한다. 선점 금지.
- 커밋 전 `bash scripts/check.sh` 통과. `.githooks/pre-commit` 이 같은 것을 돌린다.

### 9.2 비밀값

키·토큰·쿠키·비밀번호·연결 문자열·실제 고객 데이터는 **출력·문서화·커밋하지 않는다.**
설정 여부와 변수명만 기록한다. `.env.example` 에는 **형태만**, 값은 `.env`(gitignore).
**`service_role` 키는 쓰지도 저장하지도 않는다.**

### 9.3 제품

- **모아워크는 제품이고 서울경영은 첫 고객이다.** 새 워크스페이스는 **빈 상태** 다 (D71~D75).
- **«비우기» 지 «지우기» 가 아니다** — 컬럼·타입·이동규칙 등 구조는 전부 남기고 남의 회사 이름만 뺀다.
  **구조가 줄어든 PR 은 무조건 FAIL.**
- 고객 고유값(사람 이름·업체명·부서명) 을 제품 코드·프리셋·시드에 넣지 않는다.
- UI 어휘 **"회사"** · **아이템 = 탭 안의 그룹**(코드명 `sectionPreset`, 먼데이 API item 과 다름) ·
  프리셋 = 아이템 단위 구조 템플릿
- 기준 뷰포트 **1440px**. **375px 도 확인한다** (구 규약의 "면제" 는 폐기 — BBE-23 이 살아 있다).
- 브랜드 토큰 하드코딩 금지(`--mw-*`·`--sp-*`·`--fs-*` 참조) · PostHog US 리전 + 프록시
- `app_admins` 를 직접 select 하지 않는다 — `app_admin_role()` 을 쓴다.

### 9.4 목업 읽기 — 브라우저 없이

```bash
node docs/design/dump-mockup.mjs          # 전체
node docs/design/dump-mockup.mjs new      # 탭 하나 (new/contact/work/notice)
```

`file://` 로 목업을 열려고 하지 마라. 막히면 **우회하지 말고** 위 명령을 쓴다.
브라우저가 되는 세션은 열어봐도 좋다. 다만 **정본 판정은 위 명령의 출력**이다.

### 9.5 기록

- **하트비트는 Linear `BBE-94`** 에 코멘트 추가(수정 아님). *(구 규약의 `BBE-75` 는 오기다)*
- 착수: 카드 In Progress + `착수 — [세션명] · 브랜치 · base SHA · 리스 · 착수범위`
- 완주: Done + `완주 — PR #n → 머지 SHA · 배포 · health 200 · **화면 확인 증거**`
- `docs/worklog.md` 는 append-only. 기존 기록을 수정·삭제하지 않는다.

---

## 10. 폐기 — 2026-08-12

**아래는 더 이상 유효하지 않다. 과거 문서가 요구하더라도 따르지 않는다.**

| 폐기 대상 | 왜 |
| --- | --- |
| 「촬영 불가는 머지 중단 사유가 아니다」 | **2026-08-11 P0 의 직접 원인.** §3 이 대체 |
| 「375/390px 검증 면제」 | BBE-23(모바일 QA)이 살아 있다. §9.3 이 대체 |
| 하트비트 `BBE-75` | 오기. 정본은 **`BBE-94`** |
| 세션명 `CT01~10`·`GT01~10`·`G작업반장`·`MW01~10` | §2 의 `DC/NC/DG/NG` 로 대체 |
| 「마이그레이션 최신 030, 코덱스는 032부터」 | 현재 **056**. §9.1 의 «머지 직전 최신+1» 로 대체 |
| 「공급자 레인 불가침 — 코덱스 구역 카드 BBE-26·27·28·29·30·31」 | 해당 카드 대부분 완료. §2·§4 로 대체 |
| `docs/plans/배정-260810/` (42파일) | 40칸 배정. 편제 개편으로 폐기 |
| `docs/plans/배정-V6-*.md` · `slot-board.md` · `capacity-plan-*` | 〃 |
| `docs/plans/naming-rules.md` | T번호 세션명. §2 로 대체 |
| `docs/plans/onboarding-moawork*.md` | 구 온보딩. 이 문서 + 배정판으로 대체 |
| `docs/plans/DISPATCH-QUEUE-*` · `ROUTING-R1/R2-*` | 구 디스패치. `배정판.md` 로 대체 |
| `session-registry.yaml`·`dispatch-queue.yaml`·`provider-status.yaml` | SYNC-R1(2026-07-22)에서 이미 폐기 |
| `tools/dashboard.html` · `docs/plans/dashboard.html` | 총괄이 만든 중복본. 정본은 `tools/board/board.template.html` |

---

## 11. 개발 명령

```bash
npm install                          # node_modules 가 비면 lint 가 «eslint 없음» 으로 실패한다
npm run dev
bash scripts/check.sh                # lint + typecheck + test
node docs/design/qa-mockup.mjs       # 목업 자기검사 (앱은 검사하지 않는다 — §1.2)
node docs/design/dump-mockup.mjs     # 목업을 글로 읽기
```

---

## 12. 개정 이력

| 날짜 | 내용 |
| --- | --- |
| 2026-08-12 | 전면 개정. 종합 진단 반영 · 40칸→24세션 · 완료의 정의 개정 · 라벨 배정 · 3중 방어 |
| 2026-08-09 | 운영 규약 (폐기 — §10) |
