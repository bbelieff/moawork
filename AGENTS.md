# AGENTS.md — 모아워크 개발 방식

2026-09-15 개정. Astra를 포함해 이 저장소에서 일하는 모든 에이전트에 적용한다.
사용자 관리 지침이며 플랫폼 권한·보안 정책을 대체하지 않는다.

## 0. 시작과 완료

- 사용자의 목표·범위·완료조건을 파악하고 이미 허용된 조사·구현·검증을 바로 진행한다. 질문은 결과를 크게 바꾸는 미확인 정보에 한정한다.
- `CLAUDE.md`에서 제품·데이터 규칙을 읽는다. 해당 디렉터리의 추가 지침과 필요한 스킬만 읽는다.
- 현재 브랜치·변경 파일·관련 Issue/PR·활성 작업을 확인한다. 기존 구현과 사용자 의도를 비교해 중복 작업을 피한다.
- 한 책임자가 조사·구현·통합·검증을 끝까지 맡을 수 있다. 역할명 때문에 코드를 못 고치거나 별도 창의 승인을 기다리지 않는다.
- 사용자가 맡긴 범위에 포함된 병합·배포는 기존 승인으로 진행한다. 특정 승인 문구를 단계마다 요구하지 않는다.
- 완료는 요청 범위로 판단한다. 제품 출시 요청은 main 병합·운영 배포·실제 동작 검증까지, 문서/지침 변경은 저장·적용 경로 검증까지다. 하지 않은 검증은 성공으로 보고하지 않는다.

## 1. 지침의 소유와 우선순위

- 개발 방식은 이 파일, 제품·저장소·품질 기준은 `CLAUDE.md`가 소유한다. 복사본에 규칙을 중복 추가하지 않는다.
- 최신 사용자 지시가 오래된 사용자 관리 규칙과 충돌하면 최신 지시를 적용한다. 플랫폼 권한과 보안 경계는 별개다.
- 제품 판단은 최신 명시 지시 → issue/date/rationale가 있는 시각 override → 목업 v6 → 일반 제품 규칙 → 역사 기록 순이다.
- `docs/**`, 과거 핸드오프·라운드·사고 기록의 승인 대기·담당자·배포 방식·숫자를 현재 명령으로 재사용하지 않는다. 실행 중인 검사기와 현재 계약은 실제로 확인한다.
- 고정 모델 성능·가격·충전 요일을 규칙으로 삼지 않는다. 모델 설정은 사용자가 선택한 값을 유지하고 변경 필요성이 있을 때 현재 공식 문서와 실제 결과로 판단한다.

## 2. 실행과 위임

- 단독 작업은 직접 끝낸다. 독립된 조사·초안·검토를 나누면 효과가 있을 때만 위임한다. 매 작업에 코디네이터·워커·검수자를 새로 세우지 않는다.
- 사용자의 Muse Spark 구독 위임 선호를 따른다. 반복 비교·초안·독립 검토는 실제 Muse에 우선 맡기고, 책임자는 구현 판단·적용·최종 검증을 맡는다. 약 2:8은 작업량 목표이며 정확한 비율이나 대기 조건이 아니다.
- Muse에는 과업 ID·범위·필요한 최소 자료·실제 모델 및 사용량 반환 조건을 전달한다. 코드 공개 여부·전송 권한을 확인하고 비밀값·실제 고객정보를 보내지 않는다. 위임 결과를 부모가 검증한다.
- 공식 구독 경로를 사용하고 별도 API 과금으로 전환하지 않는다. Windows의 검증된 실행기는 `C:/Users/Belief-desktop/.agents/tools/muse_subscription.py`다. 접근 불가 시 실제 원인을 기록하고 허용된 작업을 계속한다.
- 도구가 제공하는 실제 모델·성공/실패·시간·토큰만 보고한다. 미제공 토큰은 측정 불가로 적는다. 누적/부모·자식 사용량을 중복 합산하지 않으며 최종 답변 전 집계 시점을 표시한다.
- 사용자 추가 지시(2026-09-15): 사용량 표에 모델별 직접 수행한 작업 목록과 전체 토큰 대비 비중을 함께 적는다. 실측이 없으면 전달·반환 텍스트의 공개된 계산 방법으로 추정한 토큰을 별도 표시할 수 있다. 실측/추정 혼합 분모와 제외된 내부 문맥·추론 등 계측 한계를 명시하며 추정치를 제공자 실측으로 표현하지 않는다. 토큰 비중을 작업 기여도나 품질 비중으로 부르지 않는다.
- 한 파일의 활성 작성자는 한 명이다. 다른 작업과 실질적으로 충돌하면 범위·의도·현재 담당자를 확인하고 조정한다. 단순히 같은 문서를 참조했다는 이유로 중단하지 않는다.
- 현재 환경의 도구를 사용한다. Orca 절차는 실제 Orca 실행에만 적용한다. GitHub 댓글은 에이전트를 깨우는 명령이 아니다. 외부 메시지는 사용자가 허용한 대상·범위에서만 보낸다.

## 3. 검증과 화면

- 변경이 깨뜨릴 수 있는 동작을 중심으로 검증한다. 구현을 그대로 따라 쓰는 무의미한 테스트나 같은 검사의 반복을 늘리지 않는다.
- 문서/지침만 바뀌면 내용·경로·상충 규칙·로딩을 확인한다. 화면 QA와 운영 재배포는 `N/A (docs-only)`로 기록한다.
- 화면 변경은 영향을 받는 실제 화면·행동·저장 결과를 확인한다. 반응형 변경은 `1440×900`·`375×812`에서 확인한다. 앱의 구조·시각 계약이 적용되는 범위에서는 해당 검사와 두 뷰포트 증거를 갖춘다.
- 새 화면·주요 구조 변경은 사용자가 판단할 수 있는 목업으로 방향을 맞춘다. 이미 확정된 디자인의 구현·오류 수정·문구 교정마다 새로운 디자인 승인 절차를 만들지 않는다.
- 목업 자기검사나 문자열 존재는 제품 화면 검증을 대신하지 않는다. 화면을 볼 수 없으면 다른 가능한 도구/담당자로 확인하고, 끝내 못 봤으면 그 부분을 미검증으로 남긴다.
- 기존 CI·보안 검사·시각 계약을 무력화하지 않는다. 실패 시 실제 원인을 고치거나 정확한 차단 조건을 보고한다. 필요한 검사가 통과한 뒤에는 새로운 변경·실패·위험이 없는 한 반복하지 않는다.

## 4. 작업 기록

- 제품 개발의 배정·진행은 `bbelieff/moawork` GitHub Issues + Project #1을 사용한다. Linear는 읽기 전용 역사다.
- 기존 Issue를 재사용하고 없으면 책임자가 만든다. 번호가 없다는 이유로 이미 허용된 조사·재현·지침 정리를 멈추거나 사용자에게 카드 생성을 떠넘기지 않는다.
- 제품 구현 범위, 담당자, 수용조건, 관련 PR, 검증 결과와 남은 문제를 기록한다. 판에 등록되지 않은 실제 작업을 숨기지 않는다.
- 현재 범위를 벗어난 결함은 기존 Issue를 검색해 연결하거나 후속 등록한다. 후속 카드 수를 채우려고 제품 범위를 확대하지 않는다.
- 요청된 핸드오프·비교표·실행에 필요한 문서는 만든다. 중복 계획서·상태 복사본을 늘리지 않고 기존 기록을 먼저 갱신한다.

## 5. 독립 검토

- 권한·인증·결제·데이터·배포 경계, 넓은 변경 또는 판단이 어려운 변경에는 독립 검토를 받는다. 작은 문서·문구 변경마다 별도 에이전트를 필수로 세우지 않는다.
- 검토자는 수용조건·실제 diff·검토한 SHA·관련 증거를 보고 판단한다. 작성자의 결론을 받아 적는 검토는 독립 검토가 아니다.
- 미해결 P0/P1은 관련 릴리스를 막는다. P2/P3는 후속 기록을 남기고 현재 수용조건 충족 여부를 판단한다. 보안·데이터 손상을 등급 이름으로 무시하지 않는다.
- 동일 GitHub 계정의 에이전트 검토를 독립된 GitHub 계정의 formal approval로 꾸미지 않는다. 코드 검토 증거와 GitHub 승인 상태를 구분한다.
- takeover/supersedes PR은 원본 SHA와 미해결 findings·처리 결과를 함께 인계하고 `merge-pr.mjs`의 기존 기계 검증 형식을 따른다. 검수 미실시와 findings 없음은 다르다.
- 검토할 때만 `docs/playbooks/subagent-review.md`의 간단한 양식을 사용한다.

## 6. 지속성과 인수인계

- 추가 사용자 메시지는 진행 중 목표에 반영한다. 취소·방향 전환이 명확하지 않으면 완료한 작업과 기존 범위를 유지한다.
- 세션이 바뀌면 현재 소스·GitHub·운영 상태를 확인하고 남은 지점부터 이어간다. 과거 보고만 보고 재구현하거나 운영 성공을 단정하지 않는다.
- 중단할 때는 완료한 것·남은 것·브랜치/SHA·미해결 findings·재개 조건을 남긴다. 독립적으로 계속할 수 있는 일은 진행한다.
- 새 지침은 파일 저장, 프로필 로드, 실제 행동 검증을 구분한다. 실행 중인 다른 앱을 임의 종료하지 않는다.

## 7. Git과 배포

- 현재 작업의 변경을 보존하고 최신 main과 비교한다. 제품 코드는 독립된 작업 공간에서 작성한다. 문서 전용 작업은 충돌 없는 기존 작업 공간을 재사용할 수 있다.
- 자신의 변경에서 기계적으로 해결 가능한 충돌은 의도를 보존해 해결하고 관련 검증을 다시 한다. 사용자 변경이나 제품 의도가 충돌하면 차이와 권고안을 제시한다.
- 실제 pre-commit 및 PR CI는 통과해야 한다. `node scripts/merge-pr.mjs <PR번호>`로 검증된 head만 병합한다. 직접 merge로 검사기를 우회하지 않는다.
- 운영 배포가 포함된 요청은 기존 VPS 경로를 이용하고 source SHA·readiness·영향받는 실제 동작을 확인한다. 문서 변경을 배포 작업으로 확대하지 않는다.
- 사용자의 추가 결제 없음 원칙을 유지한다. 다른 프로젝트가 함께 쓰는 VPS·서비스·인증정보를 보호한다. Vercel 복귀나 새 인프라 도입을 임의로 선행조건으로 만들지 않는다.

## 8. 확인이 필요한 경계

- 기존 승인 범위 안의 구현·검증·병합·배포는 재승인하지 않는다. 인증 관련 코드를 읽거나 테스트한다는 이유만으로 멈추지 않는다.
- 새 비용·제공자/공개 대상·권한 확대·비가역 운영 데이터 변경·외부 발신의 권한이 기존 요청에 포함되지 않았으면 해당 차이·위험·권고안을 설명하고 확인한다. 이미 명시된 대상과 범위는 다시 묻지 않는다.
- provider 발송 설정, worker 자격증명, hosted migration, 실제 발송 등 기존 실행 잠금은 적용 대상과 승인 여부를 확인한다. 승인 없는 잠금을 임의 해제하지 않는다.
- 실제 도구 권한 거부는 우회하지 않는다. 차단된 행동·출처·재개 조건을 밝히고 안전한 나머지 작업을 계속한다.

## 9. 데이터와 자원

- 고객 고유값·비밀값·테넌트 격리·권한 규칙은 `CLAUDE.md`를 따른다.
- 스키마 변경은 새 migration 파일로 추가한다. 이미 적용된 migration을 고쳐 쓰지 않는다. 번호·원격 객체 상태·적용 대상은 실행 직전에 확인한다.
- 남의 변경을 stage/reset/force-push/delete 하지 않는다. 불확실한 프로세스·워크트리를 정리 대상으로 추정하지 않는다.
- 자신의 임시 자원만 정리한다. 워크트리 삭제 전 실제 경로·변경·활성 작업을 확인하고 Git의 worktree 제거 명령을 사용한다. 작업 중인 세션의 공간은 완료 후 접근을 보장한다.

## 10. 폐기한 강제 절차

역할별 구현 금지, 모든 작업의 Orca Dispatch 의무, 무조건 새 워크트리, 충돌 시 무조건 중단, 모든 변경의 별도 검수자·전체 화면 QA, 매 단계 새 승인 문구, 매 수정의 다안 목업 승인, 문서 일괄 금지는 더 이상 적용하지 않는다. 과거 문서의 같은 문구로 이를 되살리지 않는다.

## 11. 보고

한국어로 결과·근거·남은 한계를 먼저 전한다. 구조/선택이 중요할 때 표나 그림을 쓰고, 단순 결과는 짧게 쓴다. 긴 작업에는 의미 있는 진행 상황을 알린다. 변경 저장·테스트 통과·병합·배포·실제 화면 확인을 각각 구분하고 바로 열 수 있는 링크를 남긴다.
