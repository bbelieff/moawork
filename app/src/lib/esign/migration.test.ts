import { readFileSync } from "node:fs";import { resolve } from "node:path";import { describe,expect,it } from "vitest";
const sql=readFileSync(resolve(process.cwd(),"../supabase/migrations/073_esign_delivery.sql"),"utf8");
describe("BBE-115 migration",()=>{it("is tenant scoped, replay safe, audited and least privilege",()=>{expect(sql).toMatch(/primary key\(org_id,event_id\)/i);expect(sql).toMatch(/for update/i);expect(sql).toMatch(/m\.status='active'[\s\S]*o\.status='active'/);expect(sql).toMatch(/provider_accepted[\s\S]*provider_document_id[\s\S]*provider_signing_url/i);expect(sql).toMatch(/electronic_contract_date/i);expect(sql).toMatch(/insert into public\.activities/i);expect(sql).toMatch(/from public,anon,authenticated/i);expect(sql).toMatch(/to service_role/i);});});
