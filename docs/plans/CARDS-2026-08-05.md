# Linear 카드 발행 요청 — 2026-08-05 (디스패치 수행)

## 0. **P0 · 신규 회사 첫 진입이 잠금 화면** — 최우선 (2026-08-05 실측 재현)

- 재현 절차(실데이터): `/platform/organizations`에서 "테스트"(/w/test) 승인 → 회사 생성·대표 연결 정상
  (DB 확인: orgs 1행, org_members owner=beliefkimkim, 06:30:38) → `/w/test` 진입.
- 결과: 화면 전체가 **`🔒 대시보드(core.dash) — 현재 플랜에서 잠긴 기능입니다 (Phase 2)`** 한 줄.
  사이드바·탭·안내 없음. 승인 화면이 약속한 "3. 회사 업무 시작 — 요청자가 대표로 연결되어
  사용자 모드로 들어가요"와 정면 모순.
- 영향: **신규 고객사의 첫 화면이 잠금 메시지**다. 온보딩이 성립하지 않는다.
- 추정 원인: 신규 조직 생성 시 기본 entitlement(플랜 기능 권한) 시드 누락 또는 오분류
  (기존 QA의 "결함 B · core.dash 잠금 배너 · 시드 오분류" 가설과 일치 — 이제 실데이터로 확정 재현).
- acceptance: 새로 승인된 회사에 대표가 진입하면 잠금 문구가 아니라 사용 가능한 첫 화면
  (대시보드 또는 대체 시작 화면)이 뜬다. 잠긴 기능은 화면 전체가 아니라 해당 영역만 잠금 표시.

## 1. P1 · 로그인 계정(이메일) 화면 미표시 — **우선 발행**

- 우선도: P1 (진단 지연의 실제 원인 — 오늘 수 시간 소모)
- 증상: 계정 메뉴·내 계정 화면 어디에도 현재 로그인 이메일이 표시되지 않음(표시명만 노출).
  다계정 사용 환경에서 "지금 어느 계정인지"를 확인할 수단이 없다.
- 실증 사례: 2026-08-03 QA(노트북, 배율 2배)에서 관리자 화면 차단으로 관측된 건이,
  2026-08-05 데스크탑 크롬 실측에서 **정상 진입**으로 재현 불가 판정.
  두 환경의 로그인 계정을 화면에서 확인할 수 없어 원인 규명이 지연됐다.
- 요구사항: ① 계정 메뉴에 이메일 상시 표시 ② 내 계정 화면에 이메일 명시
  ③ 계정 전환·로그아웃 진입점 노출 ④ 관리자/일반 모드 배지 표시
- acceptance: 로그인 상태에서 이메일이 화면에서 즉시 확인되고, 다른 계정으로 전환 시 값이 바뀐다.

## 2. P2 · 비인가·비멤버 라우팅 비일관

- 증상: 비멤버 사용자가 `/w/test1`로 안내됨. 로그인 전 `/login?error=membership`,
  로그인 후 무언 리다이렉트, 하위 경로는 안전 정지 화면 — 3종 처리가 혼재하며 ROUND-33 계약과 다름.
- acceptance: 비인가·비멤버 접근의 목적지와 안내 문구를 한 가지 계약으로 통일.

## 3. P1 · 멤버 목록 표시 오류 (기존 제안분)

- 증상: 승인자가 "보호된 대표"로 표시됨. DB는 정상(테스트1 owner = 요청자 belief@toktokhan.dev).
- 증거: 스크린샷 ss_9095p3y1j + Q3 쿼리 대조.

## 4. P2 · 조용한 실패 구분 (FIX-SILENT-DENY)

- 증상: 권한 없음(not_platform)과 장애(unavailable)가 같은 화면으로 처리돼 진단이 불가능.
- 요구사항: 오류 코드 분리(`/?error=platform-unavailable`) + 서버 로그 노출.

---

※ P0-1(관리자 화면 차단)은 2026-08-05 데스크탑 실측에서 **재현 불가** — 카드 발행 보류.
   판정 정본: `_인수인계_MWC/P0판정결과_2026-08-05.md`
