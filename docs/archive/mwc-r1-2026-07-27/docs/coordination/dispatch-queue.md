# dispatch-queue (📮 디스패치)

> **오케스트레이터만 수정.** 각 트랙은 여기서 자기 배정만 읽는다. 배정 전 **귀속 실측**(이미 완료된 일 재배정 금지 — 플레이북 §4).
> **활성화 게이트:** 아래 트랙 배정은 **잠정**. 「기획 v0.2 확정 + 레포 GitHub 푸시 + Supabase 프로젝트 생성」 3가지가 충족되면 활성.

## 트랙 로스터 (10개 · 전 트랙 범용)
동시 활성 4~5 권장, 나머지는 예비대(유휴 비용 0). **T01은 공용부라 단독 선행.**

| 트랙 | 잠정 역할 (1스프린트) | 영역(entitlement) | 선행 |
|---|---|---|---|
| **T01** | 기반·계약(PR-0): 모노레포(app/worker/supabase)·레이어 골격·`check.sh` 게이트·CI·SSOT 4문서·`.githooks` | 공용부 | 없음(최선행) |
| **T02** | `core.crm` — 신규고객/컨텍/업무 보드 미러 + 파이프라인 + 단계 이동 자동화 | 기둥① | T01 |
| **T03** | `core.org` + RLS 멀티테넌시 + 구글 OAuth | 기둥④·보안 | T01 |
| **T04** | `core.files` 문서함 + contracts 상태 + `core.dash` 기본 대시 | 기둥⑤ | T01 |
| **T05** | `core.custom` 커스텀필드·선택지·저장뷰(먼데이 컬럼 재현) | 기둥① | T01,T02 |
| **T06** | `mod.notify` 알림톡/문자 + VPS 워커(발송 잡, pg-boss) | 기둥⑤ | T01 |
| **T07** | `mod.perf` 성과·인센티브(집계·리더보드·활동량) | 기둥④ | T01,T02 |
| **T08** | `mod.hometax` 홈택스 조회→발행 + 워커 잡 | 기둥⑤ | T01,T06 |
| **T09** | `ind.policyfund` 업종팩(진행기관·상품 60여종·지역·수식) + settlements 정산 | 기둥③⑥ | T01,T02 |
| **T10** | **게이트키퍼** — 배포마다 parity·측정·RLS 침투테스트·완료판정(구현 자기보고보다 우선) | 검증 | 상시 |

## 활성 순서 (웨이브)
- **W1** T01(단독 선행) → **W2** T02·T03·T04·T09 → **W3** T05·T06·T07 → **W4** T08 · T10 상시.

## 지금 배정 (활성) — 갱신 2026-07-21 by 기획-Cowork
> **활성화 게이트 3/3 충족**(기획 v0.2·GitHub 푸시·Supabase). 인프라 완료: **Supabase(001~004 적용·검증) · Vercel 배포(www.moa-work.com 라이브) · 도메인**. 아래는 **디스패치 세션이 코드트랙에 배분**할 백로그(자율 루프). 각 항목 상세 프롬프트는 `docs/coordination/next-prompt_*.md`.

### 우선순위 백로그 (post-004)
| # | 트랙 | 작업 | 선행 | 수용기준 |
|---|---|---|---|---|
| B0 | 공용/T01 | **Vercel 워크어라운드 회수**: `app/package.json` devDeps에 `vitest` 추가(또는 tsconfig에서 vitest.config 제외) → Vercel Install Command 오버라이드 제거 | — | main 머지 후 Vercel 기본 설정으로 빌드 성공 |
| B1 | T03 | 앱 셸 v0.3 반영(1단 사이드바 10메뉴·브랜드 v1.0 토큰·다크/라이트) + Supabase env·구글 OAuth·**실DB RLS 침투테스트** | Supabase✅ | 다른 org 데이터 0건, 다크모드 토큰 일치, 로그인→홈 |
| B2 | T02 | core.crm를 **실 Supabase repo**로(로컬 어댑터→SupabaseRepo), 신규/컨택/업무 단계필터 화면 | B1 | 실DB로 파이프라인 CRUD·단계이동 |
| B3 | T05 | `core.custom` + **status 타입 컬럼 UI**(G7: 색라벨·is_done) + `board_views` 저장(G2) + 하위아이템(G1) | B2 | 컬럼 자유설정·상태클릭·뷰 저장 동작 |
| B4 | T09 | settlements 수식 화면 + 업종팩 seed(002) 연결 + `board_automation_rules`(G8 상태→그룹 이동) | B2 | 수수료·D+180 자동, 상태변경 시 이동 |
| B5 | T07 | `mod.perf` 이달의 계약회사(KPI)·리더보드 | B2 | 월 스냅샷 집계·표시 |
| B6 | T10 | 상시: 배포마다 parity·RLS 침투·완료판정. B1~B5 각 PR 검수 | 상시 | 게이트 통과분만 머지 |
| B7 | T06/T08 | 〔Phase 2·벤더〕 알림톡·홈택스·리드인입 워커. **지금은 스키마만 존재**(entitlement OFF) | 벤더계약 | 보류 |

### 실시간 배정판 (2026-07-21, 사장님 지시: 유휴트랙 병렬 투입) — 상세 프롬프트 `next-prompt_B0-B1.md`·`next-prompt_B2-B7.md`
| 트랙 | 배정 | 상태 | 병렬 착수 | 데이터 통합 선행 |
|---|---|---|---|---|
| T01 | B0 Vercel 회수 | 작업중 | — | — |
| T03 | **B1b 구글 로그인 버튼 배선(최우선)** | **배정 2026-07-22** | 지금 | 없음(인프라 완료) |
| T03 | B1 셸·auth·RLS(키스톤) | 작업중 | — | — |
| T02 | B2 crm 실repo+파이프라인 | **배정** | 지금(인터페이스 선구현) | B1 |
| T05 | B3 커스텀필드+status UI+뷰+하위아이템 | **배정** | 지금(더미라벨) | B2·기획2seed |
| T09 | B4 정산+G8 상태→이동 | **배정** | 지금 | B2·기획2프리셋 |
| T07 | B5 이달의계약회사 KPI | **배정** | 지금(집계IF) | B2·B4 |
| T04 | 공지사항(003)+홈 대시위젯 | **배정** | 지금(데이터독립) | B1(배치) |
| T06 | B7부분 worker 스캐폴드 | **배정** | 지금(완전독립) | — |
| T08 | standby | 대기 | 벤더계약 후 | — |
| T10 | 상시 검수 | 대기중 | B1~ PR마다 | — |
| 기획2 | 002seed(status라벨+hex)·G8프리셋·자동화19표·PLAN동기화·gen types·토큰SSOT | **요청** | 지금 | — |
> 병렬 원칙: 착수는 전부 지금 가능(인터페이스/로컬시드 선구현). "선행"은 실데이터 배선 시점만. 공용 index/타입 편집은 worklog 선언 후 직렬.

---

## ★ C배치 — 디스패치가 코드트랙에 배분할 것 (2026-07-27 등재, MWC)

> 상세 프롬프트: `docs/coordination/next-prompt_C-batch_셸UX-어드민-분석.md`
> **C0·C1은 이 큐에 없다** — belie 지시로 코덱스에 직접 전달됨(디스패치 관여 없음).
> 아래 4건만 디스패치 소관이다.

| # | 트랙 후보 | 작업 | 병렬 | 선행 |
|---|---|---|---|---|
| **C5** | T06 | **PostHog 연동** — SDK + 리버스 프록시(**기존 `app/src/proxy.ts`에 경로 추가**, Next16 규약) + 이벤트 10종 화이트리스트 + **PII 스크러빙** + 리플레이 마스킹(`maskAllInputs`·`data-pii`·회계/홈택스 녹화 제외) | **지금 가능** | 없음(완전 독립) |
| **C4** | T07 | **어드민 콘솔 P0 + 활동지표** — `/platform` 셸·회사 목록(메타만)·가입요청 흡수 + `platform_metrics_daily` **야간 배치 롤업**(DAU/WAU/MAU·스티키니스·휴면14일·TTFV·리텐션). 실시간 집계 금지 | **지금 가능**(지표 배치부터) | 화면은 C1 메뉴 |
| **C2** | T04 | **알림 뱃지 + 소식창** — `notifications` 테이블·벨 패널(탭 2)·Realtime+60초 폴백. **숫자=할 일(화면 진입만으로 안 사라짐)/점=변화** 규칙을 테스트로 고정 | C1 이후 | C1 스위처 컴포넌트 |
| **C3** | T05 | **지원 위젯 + 초대형 위임** — `support_threads`/`support_messages`/`access_grants`/`access_events`, 위임 2시간 기본, **멤버 개시는 읽기전용 고정**, 진행 중 고객 화면 배너, 홈택스 항상 차단 | C2 이후 | C2 알림 인프라 |

**공통**: 마이그레이션 번호는 **최신 main 기준 코덱스/트랙이 배정**(MWC 008·009는 보류 처리됨). RLS는 `is_org_member()` 무수정 + OR 조건 별도 정책. 브랜드 토큰 하드코딩 금지. 375px 무깨짐.

**⚠️ 선행 차단**: Production 로그인 권한 P0(Owner도 "회사 역할 확인 중" 고착)가 미해소면 C2·C3·C4 화면 검증 불가. 근거 `docs/evidence/PRODUCTION-ENTRY-REGRESSION-01.md`.

**MWC가 부품을 내려주는 항목** — 트랙은 서버·DB·배선에 집중하면 된다:
`dev-drop/` 폴더에 MWC가 완성 컴포넌트를 계속 드롭한다(적용법 `dev-drop/README_코덱스_적용안내.md`). 현재 C1 스위처 3파일 + 테스트 완료.

---

### 루프 규칙 (belie 지시 2026-07-21)
1. **기획-Cowork**: 확정된 요구를 스키마/문서/프롬프트로 만들어 이 큐에 넣는다(작성→pglast/tsc 검증→등재).
2. **디스패치 세션**: 이 큐를 읽고 코드트랙(T0x)에 배분, 결과를 worklog에.
3. **T10**: 완료판정. 통과분만 main 머지 → Vercel 자동배포.
4. 겹침 방지: 착수 전 session-registry 자기 행 갱신 + 기존물 검색(coordination README §다중 세션).

- **T01~T10: 활성 가능**(게이트 충족).
