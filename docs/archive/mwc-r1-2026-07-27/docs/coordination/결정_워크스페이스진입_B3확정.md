# [결정] 워크스페이스 진입 화면 = **B-3 하나씩 묻기 (One Question)**

> 결정자: belie · 일시 2026-07-27 · 기록: MWC
> 이 결정으로 `ROUND-31`의 `USER_VISUAL_SELECTION_READY / PRODUCT_HOLD`가 **해제**된다.

---

## 코덱스에 전달할 내용 (그대로 복사)

```
[belie 결정 — WORKSPACE-ENTRY-B-FAMILY-USER-DECISION-03]

선택: B-3 "하나씩 묻기" (One Question)

근거 후보: brand/MoaWork_Workspace_Entry_B_Family_4Variants_v0.3.html
           (52,654 bytes / SHA-256 DE2418DC09477A531C695F58C35D5070DAC16BDE28279331992266352D3E68D9)
route contract: docs/design/round-30/02-workspace-entry-b-routing-contract.md

B-3 요지(시안 문구 그대로):
  "필요한 질문만 하나씩 — 라우팅이 확정되면 질문 0개,
   선택이 필요하면 현재 질문 하나에만 집중해요."

→ ROUND-31의 HOLD 중 '사용자 B-1/B-2/B-3/B-4 선택' 해제됨.
→ 나머지 HOLD(product server redirect / callback·session·RLS·DB /
   production deployment / implementation·merge·deploy)는 코덱스 진행 사항.

belie 우선순위:
  1) feat/public-workspace-entry → main 머지 + 배포  (로그인 + 진입 화면)
  2) 그 위에 B-3 제품 구현
```

---

## 왜 B-3인가 (기록용)

- 라우팅이 서버에서 확정되면 **질문 0개**로 바로 진입 → 최단 경로.
- 선택이 필요한 예외 상황에서만 **질문 1개**에 집중 → 5인 이하 조직·비개발자 사용자에게 인지 부담 최소.
- 기획 정본의 UI 원칙(토스식 "쉬움", 클릭 최소)과 일치.

## 후속 (코덱스 판단 영역)

ROUND-31 명시대로, 선택 이후에도 다음이 필요하다:
새 writer lease · implementation contract · current hash 검수 · 사용자 실제 화면 승인 · merge/deploy gate.
