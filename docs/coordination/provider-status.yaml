# 프로바이더(외부 연동 / 실행 주체) 상태 — SSOT
# status: planned | provisioning | live | blocked
# 비밀값(URL·키·비밀번호)은 여기에 기록하지 않는다. 환경변수로만 주입.
version: 1
updated: 2026-07-21

providers:
  - name: supabase
    kind: db+auth
    status: planned
    notes: PostgreSQL DB + Auth. URL/anon key 는 env 로 주입.

  - name: monday
    kind: mirror-source
    status: planned
    notes: 서울경영지원센터 먼데이(monday.com) 보드 — 복제 대상 원본.

  - name: vps-worker
    kind: worker-host
    status: planned
    notes: pg-boss 잡 러너를 구동할 VPS 호스트.
