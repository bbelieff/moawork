# 컬럼 표준 · 마이그레이션 매핑 v1

> belie 지시(2026-08-10): *"컬럼의 이름 기준은 들어있는 값으로 하자. 먼데이 칸의 기존 이름은 무시."*
> 작성: 모아워크 데탑 C작업반장(260809)
> 근거: `먼데이-전체스키마-v1.md`(구조) + 2026-08-10 보드 실측(값) + `MONDAY-AUTOMATION-EVIDENCE-01.md`

---

## 0. 원칙

1. **이름은 값이 정한다.** 먼데이 컬럼 제목은 참고만 하고, 실제로 무엇이 들어 있는지로 우리 컬럼을 정한다.
2. **한 칸에는 한 종류만.** 지금 먼데이는 한 칸에 두세 종류가 섞여 있다(§2). 우리는 쪼갠다.
3. **비어 있는 칸은 옮기지 않는다.** 값이 있는 칸에서 끌어온다.
4. **자동 교정은 규칙이 명확할 때만.** 애매하면 «확인 필요» 로 남기고 사람이 판정한다.

---

## 1. 실측 — 먼데이 신규고객 보드의 실제 값

2026-08-10 보드 조회에서 확인한 것. **컬럼 제목과 내용이 어긋나 있다.**

| 먼데이 칸 | 실제로 들어 있는 값 | 판정 |
| --- | --- | --- |
| `Name` | `도소매업` · `기타_서비스업` · `일반음식점/배달전문점` · `서비스업(음식,카페)` | **업종** |
| `회사명` | 대부분 비어 있음 | 미사용 |
| `주소` | `좋은계란` · `하늘소리` · `다산컨설팅` · `만물상회` · `냐냐샵` | **상호(회사명)** |
| `매출액` | `개인사업자` · `6,000만원_~_9,000만원` · `1,000만원_~_2,000만원` | **사업자 유형 + 매출 구간이 섞임** |
| `사업자 유형` | 비어 있음 | 미사용 |
| `대표자명` | `서영진` · `이미경` · `안정호` | ✅ 맞음 |
| `연락처` | `01024818230` · `010..33484767` | ✅ 맞음 (형식은 지저분) |
| `출동`(people) | `박정화` / `조은혜, 박정화` — **복수** | **협업자** |
| `담당자`(status) | 이대표 / 박정화 실장 / 담당자 미정 — **단일** | ✅ 담당자 |

**교차 증거**: 「제조업DB」 뷰의 필터가 `Name = 제조업 · OEM_위탁제조 · 자사_직접제조 …` **17가지 문자열**이다.
Name에 회사명이 들어 있었다면 이런 필터를 만들 수 없다. 「음식점DB」는 31가지.

**원인 추정**: 메타 광고 폼의 필드 순서와 보드 컬럼의 짝이 어긋난 채로 오래 쌓였다.
→ **원천(광고 폼 매핑)을 먼저 고쳐야 앞으로 들어올 건이 깨끗해진다.**

---

## 2. 우리 컬럼 표준 — 신규리드 관리

한 칸에 한 종류. 이름은 값이 정한다.

| # | 컬럼 | 출처 | 비고 |
| --- | --- | --- | --- |
| 1 | **회사명** | ⟳ 수집 | 상호. 업체 마스터 식별의 기준 |
| 2 | 대표자명 | ⟳ 수집 | |
| 3 | 연락처 | ⟳ 수집 | **숫자만 저장**, 화면엔 하이픈 |
| 4 | 이메일 | ⟳ 수집 | |
| 5 | **업종** | ⟳ 수집 | 상태 8종. 제조업·도소매업·서비스업… |
| 6 | **사업자 유형** | ⟳ 수집 | 드롭다운. 개인/일반·개인/간이·법인… |
| 7 | **매출 구간** | ⟳ 수집 | `1,000만원~2,000만원` 형태. 금액이 아니라 구간 |
| 8 | **시도** | ⟳ 수집 | 광고 폼은 «서울»·«경기» 수준의 큰 덩어리만 준다 |
| 8-2 | **시군구** | ✎ 입력 | **상담하며 사람이 채운다.** 특례보증은 지자체 단위로 뜨므로 이게 없으면 재타겟팅을 못 한다 (D65) |
| 9 | 주소 | ✎ 입력 | 상담하며 채움 |
| 10 | 광고 명 | ⟳ 수집 | 캠페인·폼 이름 |
| 11 | 신청일 | ⟳ 수집 | 유입 시각 |
| 12 | **담당자** | ▼ 버튼 | **1명.** 보고선의 시작점 |
| 13 | **협업자** | ▼ 버튼 | 여러 명. 같이 일하는 사람 |
| 14 | 상담 상황 | ▼ 버튼 | 16단계. 아이템 이동의 트리거 |
| 15 | 간편 부재 안내 | ✉ 발송 | **5회 발송**(녹취 실측). 고정 간격 없음 — 재접촉일은 담당자가 달력으로 지정(D56) |
| 16 | 악성 부재 안내 | ✉ 발송 | 간편 부재와 **다른 갈래**. 반복 부재가 아니다 |
| 17 | 1차 상담 안내 | ✉ 발송 | |
| 18 | 2차 확정 안내 | ✉ 발송 | |
| 19 | 3차 결과 안내 | ✉ 발송 | |
| 20 | 피드백 상황 | ▼ 버튼 | 피드백 전·완료·정보보완·서류보완 |
| 21 | 컨택 이동 | ▼ 버튼 | 리드컨택으로 넘기는 트리거 |
| 22 | 재통화 일시 | ✎ 입력 | 날짜·시각. **달력으로 넣는다** |
| 23 | 대면미팅 일시 | ✎ 입력 | 날짜·시각. **달력으로 넣는다** |
| 23-2 | **재접촉일** | ✎ 입력 | 부재·보류·고민 건을 «언제 다시 건드릴지». 담당자가 달력으로 지정 (D56·D57) |
| 24 | 계약금 | ✎ 입력 | **금액은 원장으로 간다**(업체·자금건 모델 §2-3) |
| 25 | 상담 메모 | ✎ 입력 | 긴 텍스트 |

> `📬` 접두어는 **떼어낸다.** 우리는 출처 배지(✉ 발송)로 구분하므로 이름에 기호를 넣을 필요가 없다.
> `AI_1차`·`AI_2차 확정`·`AI_3차불가` 는 AI와 무관하다 — 실제로는 **안내 문자 발송 단계**다. 이름을 값에 맞춘다.

---

## 2-2. 날짜 칸은 전부 달력이다

**손으로 «08-11» 을 타이핑하면 형식이 사람마다 갈라진다.** `8/11` · `08.11` · `8-11` 이 한 칸에 섞이면 정렬도 필터도 무너진다. 그래서 «사람이 넣는 날짜 칸»(✎ 입력)은 예외 없이 달력에서 고른다. ⟳ 수집·ƒ 수식 날짜는 애초에 사람이 못 건드린다.

달력이 붙는 칸 — 총 11개

| 탭 | 칸 |
| --- | --- |
| 신규리드 | 재통화 일시 · 대면미팅 일시 · **재접촉일** |
| 리드컨택 | 전자계약 / 미팅일정 · **재접촉일** |
| 계약업체 실무 | 방문 및 신청 일 · 실사일 · **예상 심사 종료** · 수수료_입금일 · **조달일** · 계약금_입금일 |

> **창업년도는 날짜가 아니다.** 값이 `2019` · `2012` 처럼 연도 하나뿐이라 달력을 띄우면 오히려 방해가 된다. 숫자 칸으로 둔다.

## 2-3. 계약업체 실무 — 날짜에서 날짜가 나온다

녹취 실측: *"조달 날짜는 고객이 조달된 날짜로 설정… 거기에 따라서 D-day 365일이 생겨지게 되고"*

**재신청 안내일을 손으로 적지 않는다.** 조달일만 넣으면 나온다.

| 칸 | 출처 | 규칙 |
| --- | --- | --- |
| **조달일** | ✎ 입력 | 고객이 실제로 자금을 받은 날. 달력 |
| ƒ재신청 안내일 | ƒ 수식 | **조달일 + 365** — 1년 뒤 다시 신청할 수 있다 (D67) |
| ƒD+180 | ƒ 수식 | 조달일 + 180 — 중간 점검 |
| **예상 심사 종료** | ✎ 입력 | 녹취: *"7월 6일부터 한 달 반"*. 기관·상품마다 다르므로 사람이 넣는다 |
| ƒ심사 D-day | ƒ 수식 | 예상 심사 종료 − 오늘. 다가오면 확인 |

> 먼데이는 `재신청 안내일` 이 **손입력**이었다. 조달일이 바뀌면 재신청일이 따라 바뀌지 않아, 1년 뒤 재접촉 기회를 통째로 놓친다. 이 한 줄이 §6 D67 의 실체다.

---

## 3. 마이그레이션 매핑 — 기본값

### 3-1. 단순 이동

| 먼데이 | → 우리 | 규칙 |
| --- | --- | --- |
| `주소` | **회사명** | 값이 있으면 그대로 |
| `Name` | **업종** | 17·31가지 문자열 → 8종 업종으로 정규화(§3-3) |
| `대표자명` | 대표자명 | 그대로 |
| `연락처` | 연락처 | 숫자만 추출 |
| `출동` | **협업자** | 복수 그대로 |
| `담당자` | 담당자 | 라벨 id 보존 |
| `상담 상황` | 상담 상황 | **라벨 id 보존**(순번 아님) |
| `📬…` 5종 | 안내 문자 5종 | 접두어 제거 |

### 3-2. 한 칸 → 두 칸 (분해)

`매출액` 칸의 값을 **패턴으로 판정**한다.

```
값이 금액 구간 형태     ( 숫자 + 만원|억 + ~ )   → 매출 구간
값이 사업자 유형 목록에 있음 (개인사업자·법인·개인/간이…) → 사업자 유형
둘 다 아님                                        → 확인 필요
```

### 3-3. 업종 정규화

Name에 들어 있던 17·31가지 표기를 8종으로 접는다. 예:

```
제조업 · OEM_위탁제조 · 자사_직접제조 · ODM제조업 · 개인사업자_제조업 …   → 제조업
일반음식점 · 배달전문점 · 카페_/_음식점업 · 음식점&카페 …                  → 일반음식점/배달전문
```

**원문은 버리지 않는다.** `원본_업종표기` 라는 보조 필드에 남겨 되돌릴 수 있게 한다.

### 3-4. 회사명이 비는 경우

`주소`도 비어 있으면 회사명을 만들 수 없다.
→ **«회사명 없음»으로 표시하고 마스터에 등록하지 않는다.** 리드로만 남긴다(리드는 회사가 아니다).

### 3-5. 지역 분해 (BBE-127 · D65)

> **⏱ 이 절은 2026-08-12 조사 기준이다.** 2026-08-18 확인: `region_sido`·`region_sigungu`
> 컬럼은 `065`(2026-08-15)에 이미 있고, 아래 **레이어 B 분해 규칙을 구현한 마이그레이션은 못 찾았다.**
> 쓰기 전에 지금 스키마를 다시 확인하라.

> 상세 실측 근거는 `docs/design/BBE-127-지역분포조사_v1.md` · 정답표는
> `docs/design/지역사전_시도시군구_v1.md`. 여기서는 규칙만 정리한다.

**두 레이어로 나뉜다** — 하나는 파싱 문제가 아니고, 하나는 파싱 문제다.

**레이어 A. 신규리드(상담 전)** — 파싱 대상 아님.
D65 확정: 시도는 광고 폼에서 자동 수집(⟳), **시군구는 상담하며 사람이 직접 입력**(✎)한다.
즉 신규리드 단계에서 시군구가 비어 있는 건 정규화 실패가 아니라 **정상(미상담)**이다.
→ UI는 시도 드롭다운(⟳, 편집 불가) + 시군구 드롭다운(✎, 상담 중 사람이 채움. 선택한 시도에
종속되는 캐스케이딩 목록 — `지역사전_시도시군구_v1.md` 기준)으로 둘 다 새로 만든다. **분해 로직 불필요.**

**레이어 B. 계약까지 간 과거 데이터**(업무관리·회계 보드의 결합형 「지역」 드롭다운) — 파싱 대상.
상담사가 이미 시도+시군구를 합쳐 `경기_광주` 식 한 값으로 등록해 놓은 레거시 데이터를 두 컬럼으로 쪼갠다.

우선순위 규칙(위에서부터, 먼저 맞은 규칙만 적용):

1. **정확 매칭** — `_`로 1회 분리해 앞부분을 `지역사전`의 매칭접두사와 대조. 일치하면 시도 확정.
   뒷부분은 시도 확정 후 그 시도 소속 시군구 목록에서만 대조한다(예: "경기_광주" → 시도=경기 확정
   → 그 안의 "광주시"와 매칭. 광주광역시와 혼동하지 않는다 — `지역사전` §확인필요-1).
2. **군위군 예외** — 앞부분이 `경북`이고 뒷부분이 `군위`(또는 `군위군`)면, 시도=**대구**·시군구=**군위군**
   으로 강제 매핑(2023.7.1 개편 반영). 1번보다 먼저 적용.
3. **접미사 정규화 매칭** — 뒷부분에 시/군/구 접미사를 붙이거나 떼도 사전에 정확히 하나만 걸리면 확정.
   **두 개 이상 걸리면(모호) 확정하지 않고 6번으로.**
4. **시도만 있는 값**(`_` 없음, 예: "서울") — 시도만 채우고 시군구는 비운다. **실패 아님.**
5. **구분자가 `_`가 아닌 경우**(공백 등) — 공백을 `_`로 치환해 1번부터 재시도.
6. **분해 실패(모호 포함)** — 시도·시군구 모두 비우고, `원본_지역표기`에 원문 그대로 보존.
   자동 병합·자동 보정 금지(D40과 동일 원칙). 마이그레이션 로그에 "확인 필요"로 별도 집계.

**「손으로 채워야 하는 건」 추정치**: 레이어 A는 **미상담 리드 전체**가 이에 해당(정의상 그렇다,
D65). 정확한 건수는 실 DB에서 상담 상황 초기 단계 건을 세면 나온다(아래 §4에 세는 항목 추가).
레이어 B(레거시 옵션 사전 정리 대상)는 최소 **13건**(`BBE-127-지역분포조사_v1.md` §2-3, 축약형
중복+오기).

**절대 금지**: 실패 건 삭제·빈 문자열 덮어쓰기 금지 / 모호한 두 후보 중 임의 확정 금지 /
사전에 없는 신규 지명이 나와도 마이그레이션 스크립트가 사전을 자동으로 늘리지 않음(사람이 검토 후 갱신).

---

## 4. 착수 전 반드시 할 것 — 분포 조사

교정 규칙을 확정하려면 **8,400건이 얼마나 규칙적으로 어긋나 있는지**를 먼저 세어야 한다.

| 세어야 할 것 | 왜 |
| --- | --- |
| `주소`가 비어 있는 건수 | 회사명을 못 만드는 건수 = 마스터 등록 불가 |
| `매출액`이 금액 패턴인 건수 / 사업자유형 패턴인 건수 / 그 외 | 분해 규칙이 몇 %를 덮는지 |
| `Name`이 업종 목록에 없는 값인 건수 | 정규화 예외 |
| `회사명`에 값이 있는 건수 | 있으면 그쪽이 우선 |
| **(§3-5) 신규리드 중 시군구가 빈(=미상담) 건수** | 레이어 A 실제 규모 — 이 조사로는 산출 불가, 실 DB에서 직접 셀 것 |
| **(§3-5) 레거시 「지역」 드롭다운이 §3-5 규칙 1~5로 확정되는 건수 / 6(확인 필요)로 빠지는 건수** | 레이어 B 분해 규칙의 실제 적중률 |

**읽기 전용 조회로 건수만 뽑는다. 고객 데이터 내용은 기록하지 않는다.**

---

## 5. 되돌릴 수 있게

- 모든 이동에 `원본_컬럼` · `원본_값` 을 보조 테이블에 남긴다
- 「확인 필요」로 분류된 건은 **자동 교정하지 않고** 목록으로 남긴다
- 마이그레이션은 **미리보기 → 건수 대조 → 실행** 3단계. 미리보기 없이 실행 금지

---

## 6. 우리가 이미 정한 것들 (여기에 모음)

| 결정 | 내용 | 근거 |
| --- | --- | --- |
| 담당자 3층 | 담당자(1명) · 협업자(N) · 알림 대상(N) | 2026-08-10 |
| 금액 | 딜에 저장하지 않고 **원장**에 | `업체·자금건_데이터모델_v1.md` |
| 회사 식별 | 사업자등록번호 → 없으면 정규화 회사명+대표자명 | 〃 |
| 리드 ≠ 회사 | 계약 시점에만 마스터 등록 | 〃 |
| 연락처 | 저장은 숫자만, 표시는 하이픈 | BBE-106 |
| 상태 트리거 | **라벨 id**로 저장(순번 금지) | 자동화 실측 |
| 컬럼 배치 | 표 순서와 상세 순서는 별개, 값은 하나 | BBE-107 |
| 이름과 주소 분리 | 탭 이름을 바꿔도 URL은 유지 | BBE-103 |

---

## 7. 카드 반영

- **BBE-109**(복제본 병합)에 §3 매핑 규칙 추가 — §3-5(지역 분해) 포함
- 신규: **분포 조사**(§4) — 읽기 전용, 건수만
- 신규: **메타 광고 폼 매핑 교정** — 원천을 고쳐야 앞으로 들어올 건이 깨끗해진다
- **BBE-127**(리드 재타겟팅 선행조사, 모아워크 노트북 CT01) — §3-5·§4 지역 행 작성.
  정답표 `지역사전_시도시군구_v1.md`, 상세 실측 `BBE-127-지역분포조사_v1.md`.
  BBE-123·BBE-109·BBE-118·BBE-125에 통지 완료.
